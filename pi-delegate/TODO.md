# pi-delegate — running state

Keep this current. It is the handoff between sessions: what exists, what is proven, what is open.

## Why this package exists

Covers the delegation `pi-subagents` (125 schema params, ~14.8k lines, 11 delegation tools per turn) and `pi-strings` were doing, and **coexists with both** — distinct tool names, settings keys, session dirs, logs; verified side-by-side with pi-subagents loaded, no errors. Removing the old stack is a per-machine choice; nothing in this package requires it. Audit evidence from 856 runs in `~/.pi/agent/run-history.jsonl`: `worker` failed 34%, `code-explorer`/`code-architect` 100% (Claude-format tool names, no mapping), 1.26 review runs per implementation run, median 22 min child wall-clock per session, 183 missions with a median of 1 run each. Delegation happened in ~11% of sessions; the schema was paid in 100%.

Design rules that must hold:

- **No hacks, no heuristics.** Tools present facts as recorded. Judgment — what is equivalent, adequate, worth its price — belongs to the agent with the user, in the open. Rules of thumb go in the readable skill, never silently in code.
- **Model offerings are never deduplicated.** The same weights on a subscription, a metered API, a local box, or a free tier are different offerings (marginal cost, speed, quantization, limits).
- **No model names in prose.** Not in the skill, README, role files, or source. Role files and `delegate-models.json` are the only places a model is named.
- **Approval is a conversation.** No dialog, no modal, and no reminder text in results. The agent proposes in its own words; `delegate_ctl approve` records a default only after the user agrees. A tool never nags about a decision it does not own.

## Current state (2026-09-22)

Two tools. In `settings.packages`; `pi-subagents` and `pi-strings` removed, `settings.subagents` overrides dropped (backup: `~/.pi/agent/settings.json.pre-delegate`).

- `delegate({ role, task, model?, reason?, context?, cwd?, timeoutMs?, sync? })` — in-process child via `createAgentSession`, no extensions/skills loaded, built-in tools only. **Background by default**; use `delegate_ctl wait` when later work depends on the result. `sync: true` explicitly joins at launch.
- `delegate_ctl({ action: models|rate|approve|roles|status|result|wait|steer|cancel, ... })`.
- Roles: `scout` (fresh, read-only, low), `worker` (fork, all built-ins, medium), `reviewer` (fresh, read-only, high). All inherit the parent model unless a role file or call names one.
- Discovery, lowest → highest priority: package `roles/` → `~/.pi/agent/agents/` → `~/.agents/agents/` (skips `_*`/`.*`) → `<cwd>/.pi/agents/` when trusted.
- State: `~/.pi/agent/delegate-runs.jsonl` (plaintext task, tokens, cost, duration, changed files), `delegate-models.json` (approved defaults + catalog snapshot), `delegate-ratings.json` (agent-researched ratings, stale after 14 days), child transcripts in `<cwd>/.agents/pi/subsessions/` — with the work, not under `~/.pi`.

## Reloading onto per-run ownership broke every delegate (uncommitted)

- Field report after `eea99c6` (per-run ownership, no parent lease): every `delegate` failed with `The "path" argument must be of type string`. The agent then fell back to `pi -p` in tmux. Cause: the process-global runtime registry deliberately outlives `/reload` so live children survive it. A session that reloaded onto `eea99c6` therefore kept an Owner built by the previous code, which has no `dir`, so `join(owner.dir, …)` for the new pointer file got `undefined`. A fresh process was never affected; the suite only ever started fresh.
- Reproduced in real Pi: a worktree at `83d0e05`, one child, source switched to `eea99c6`, `/reload`, delegate again gives the exact error. The control (starting and reloading on `eea99c6`) works.
- Fix: `attach` upgrades an Owner of the previous shape in place (`adoptPreviousOwner`). It keeps the same object because live children reference it, and it sets `dir`. It stamps that parent's runs with this process as owner, so another process cannot adopt children still running here. It drops `path`/`runPaths`/`lost`, and releases the old proper-lockfile lease.
- The first version of that fix crashed real Pi on reload: `.then(release)` passed the resolved value as an argument, which proper-lockfile read as its callback (`callback is not a function`, uncaught, from an fs callback). The in-process test missed it because its fake release ignored arguments. The test now records the arity and requires zero, and fails on the buggy call.
- **Lesson:** anything in the process-global registry is foreign input across a `/reload`, exactly like persisted tool results. A change to `Owner` or `Run` shape needs a migration in `attach`, or a registry version bump if live children may be dropped.
- Checks: `npm run check` 36/36, `smoke:tui` PASS. Real cross-version reload `83d0e05` → fixed: launches after the reload, both children complete, lease lock removed, and the pre-reload run's record carries this process's `ownerPid`.

## `models` as a table; steer model that never reached the session (`ae7fa45`, `44a5b89`)

- Field report (screenshot from another machine): `delegate_ctl models` still "looks like absolute ASS". That machine was stale (its `… Ctrl+O expand` string was removed in `24f88ca`), but current code was bad too: `models` had no structured details, so it rendered the model's text. The 8-line head preview was entirely `DEFAULTS`/`RATINGS`/`OPENROUTER`/`CATALOG`, with **zero offerings visible** when collapsed.
- `models` now returns `details.kind: "models"`. The model-facing text is byte-identical: verified by diffing before and after for three filters. Only live OpenRouter uptime/order drift differed. `liveFacts`/`defaultsFacts` are the single source for both text and view.
- View (`modelsView` in `render.ts`): a header with the match count, one line for defaults and drift count, an aligned table (id, ctx, `$/M in/out`, AA intel/coding/agentic only when any row has them, rated), and per-row exceptions only (`current`, `no reasoning`, `live $x/y/M` when the registry differs, `tiered pricing`, `expires`, endpoint count). It shows six rows, then `… N more · M on OpenRouter but not in your registry, Ctrl+O to expand`. The expand adds endpoint lines, full pricing notes, labelled defaults/drift/OpenRouter/ratings/unregistered sections, and providers packed so an entry never splits.
- Narrow widths: score columns drop from the right before the id is cut; notes only get leftover space, so a clipped row never cuts into a price.
- **Pi exits on any line wider than the terminal**, and persisted records re-render at the width the terminal has after reload. Two crashes were found live at 40 cols: our providers packing appended ` ·` without reserving room (now fixed, with a test using the real catalog that fails on the old logic), and pi-vertex's startup widget ignored `width` entirely (crashed at any width < 68; now clipped). `resultView` also clamps every models line, so a future wrap bug cannot take the app down.
- Shape-checked like `roles`/`runs`: records without usable models details (all records written before this change) fall back to the text path.
- **Steer `model:` did not reach a retained session.** `beginResume` spread `...run` (including the live `session`), and `openSession` returns early when a session exists, so the next segment ran on the old provider/thinking while the record claimed the new one. The common trigger is a first request that fails (for example on a usage limit): zero turns, session still in memory, retries keep hitting the exhausted provider. Now a changed model or thinking level drops and disposes the retained session after the record is saved, and the normal open path rebinds from the transcript. The SDK honors an explicit `options.model` on open. The old test missed it because `fixture` and `fixture-alias` share a model id on one loopback, and it asserted only the record. The new regression test gives the replacement a distinct id and asserts the retry request's `model`. It failed before the fix (`actual: 'fixture'`).
- Checks: `npm run check` 35/35, `npm run smoke:tui` PASS, pi-vertex `npm test` 1/1. Real Pi 0.87.1 sessions (parent on `github-copilot/gpt-5-mini`, real registry and live OpenRouter) rendered `models "stepfun"`/`"glm"` collapsed and expanded at 110/54/40/30 cols, across `/reload`, without a crash.

## Ctrl+J is the Agents key again (`83d0e05`)

- User call: back to **Ctrl+J**. The newline they use is Ctrl+Enter, never Ctrl+J. This supersedes the Alt+J move below.
- It does not collide with their Ctrl+Enter in Ghostty. Pi negotiates kitty flags 7, and Ghostty keeps the legacy Enter byte only when no modifier is held, so Ctrl+Enter arrives as `CSI 13;5u`. `matchesKey` confirms that matches `ctrl+enter`, not `ctrl+j`. Only a terminal that sends Ctrl+Enter as a bare LF would collide: LF matches `ctrl+j`, and the extension shortcut wins over the editor.
- The startup report is back with default keybindings, confirmed live on this machine: `Extension shortcut conflict: 'ctrl+j' is built-in shortcut for tui.input.newLine`. Pi computes it against *resolved* keybindings and has no extension-side opt-out. `{ "tui.input.newLine": ["shift+enter"] }` in `~/.pi/agent/keybindings.json` removes it; the README says so. This is per machine.
- The smoke uses that documented setup and still fails on any startup issue. Its multiline check now sends Shift+Enter (modifyOtherKeys encoding, since tmux has extended-keys off) and asserts two lines.

## Startup banner and the stolen newline (superseded: see above)

- Reported: every startup printed `[Extension issues] … shortcut conflict: 'ctrl+j' is built-in shortcut for tui.input.newLine`. It had been sitting at the top of every terminal capture all along; the suite passed because nothing asserted on startup state. The banner was truthful and worse than noise: Ctrl+J is the **only** newline that works without the kitty keyboard protocol, so the override made multiline drafts impossible there — while the README claimed "Shift+Enter still inserts a newline" unconditionally. The tmux runs even warned `extended-keys is off`, and no test typed a multiline draft.
- Moved the entrance key to **Alt+J** (same letter, free of built-ins and editor bindings, xterm-standard ESC-prefixed encoding — works without kitty). Ctrl+J is the newline again on every terminal. README/skill/hint updated; the Shift+Enter claim is now qualified.
- Testing fixes, the real lesson: startup diagnostics are failures — smoke now asserts no `[Extension issues]` and no `shortcut conflict`, and types a multiline draft through Ctrl+J. It also waits for `/reload`'s own completion line (`Reloaded …extensions`) instead of passing on a persisting footer, and polls for typed text instead of asserting on an instant capture. Three assertions in this flow had been passing on pre-existing screen state.

## Crash on reload (fixed, uncommitted at time of note)

- **Reported crash:** `TypeError: Cannot read properties of undefined (reading 'length')` in the roles renderer, taking the whole app down. Root cause: tool results live in the transcript and are re-rendered after `/reload`, including records written by earlier versions whose `details.rows` lacked the `tools` field added today. The renderer trusted today's shape and read `r.tools.length` off last week's record. My smoke only ever exercised fresh results — that gap is exactly what a transcript carries and a smoke does not.
- Fix: `resultView` in `render.ts` treats every result as foreign input. Structured rendering only when rows pass a shape check (`isRoleRow`/`isRunRow`); anything else falls back to the text path, which is complete by construction. `runLine`/`resultLines`/`runTitle` no longer assume arrays or strings exist.
- Regression tests replay the exact legacy record from the crash plus malformed rows, minimal child rows and a result missing `output`. Lesson to keep: **anything that renders persisted records must be tested against the shapes already on disk**, not only what the current writer emits.

## Control-tool rendering (uncommitted)

- Field report: "delegate_ctl renders in the chat log still look like absolute shit." They did: a dim `delegate_ctl · models` label followed by up to 12 *logical* lines of raw text, which at terminal width became a wrapped wall, with `… Ctrl+O expand` that did not match Pi's keybinding display.
- Both tools dropped `renderShell: "self"`, so Pi draws its standard tool shell: same box, padding and success/error background as its built-ins. Content is ours; the frame is Pi's.
- Now Pi's own shape: `theme.toolTitle` call line with the action and its subject, output in `toolOutput`, preview clipped by visual lines with `keyHint("app.tools.expand")`. Preview keeps the head, since these reports lead with defaults/drift/counts.
- `roles` returns structured `details`. First attempt clipped each role's description into the last column, which just produced five rows ending mid-word; real user output made that obvious. The row now carries decision facts (mode, offering, writes vs read-only, tool count, custom timeout, unavailable tools) and the prose moves behind the expand with the role's tools and source. The text the model receives is unchanged. `status`/`result` on one child reuse the compact outcome line.
- Second report: still "flat and blocky, one white colour". Two causes: the shell and the content. Both tools set `renderShell: "self"`, so Pi drew no box, padding or status background — dropping it gives them the same frame as `read`/`bash` (`toolSuccessBg` confirmed in the ANSI capture). And the content was one `toolOutput` tone, so `status` was a wall of identical text.
- `runLine` in `render.ts` is now the single way a child is drawn anywhere — panel, status, outcome: status glyph and bold title in the state colour (accent running, success, error, warning), current tool in accent while running, role in muted, elapsed/turns dim, cost muted, failures warning, changed files success. The word "complete" is dropped when the glyph already says it. `statusMark` is shared with the pinned frame so the colours cannot drift apart.
- `status` without a runId returns `details.rows` and renders one styled row per child under a counted header, instead of repeating the full summary per run.
- `previewLines` in `render.ts` is the head-preview rule, unit-tested; the terminal smoke covers the roles table, the expand, and the per-run status lines in regular and fullscreen.

## Blocking joins and fork role confusion (uncommitted)

- Field report: a parent's typed message sat queued as `Steering:` while one child ran, reading as "children are not really async". The root cause was our own renderer: `renderCall` was `empty` for both tools, so a blocking `wait`/`sync` join drew **nothing** and the parent looked frozen. OMP's Task tool, by contrast, renders its call block and streams agent progress in the transcript. A blocking join now renders a live line (`⏳ Waiting for … · <child tool> · elapsed · abort to stop waiting; the child keeps running`) that gives way to the outcome; async dispatch stays silent, as decided earlier. Launching does not block — background `delegate` returns the run id immediately and tests cover it. What blocks is a deliberate join: `delegate_ctl wait` or `sync: true`. The Agents frame also says `parent blocked in wait` while any waiter is attached (a sync launch counts while it runs), `status` reports `joinedWaiters`, and both docs say aborting the tool detaches the waiter without touching the child. Proven in real regular and fullscreen Pi by a model-driven sync launch: waiting line present, frame marked, outcome recorded exactly once afterwards.
- Field report: a forked child "believed it was the parent monitoring a worker", inspected the job and checked a PID instead of doing the work. Stripping delegation tool calls (previous section) removes the mimicry but not the parent's prose, which still reads as supervision. Forked children now get an explicit clause in their own instructions: the preceding conversation belongs to the agent that delegated to them, they are not observing anyone, and the assignment is theirs. Asserted in the fork test against the real system prompt. Prose-level confusion is reduced, not provably eliminated — for a parent whose recent context is mostly orchestration, `fresh` with a full brief remains the safer call, and both docs now say so.

## Long-running children (uncommitted)

Field report: a model told its user "pi-delegate only wakes me on final completion, not mid-run progress pings like the old tool did. I'll report the outcome once it finishes (up to ~8.5–9 hours)" — planning to idle for nine hours, on a child whose default budget would have killed it after fifteen minutes.

Single wake-up stays: an interrupt per tool call spends a parent turn on unrequested information, and the human already has the live frame. What was missing is the stated path and an honest budget.

- `status` is now a real progress read: `now: bash · 12 tool calls so far · 47 min of its budget left`, instead of an optional `last tool:` line.
- The skill says waiting is never the parent's job — do other work, `wait` when nothing else can proceed, take one `status` read when asked; a loop is still forbidden, one read is not.
- `timeoutMs` carries its default in the schema description, and a timeout explains itself: the budget it hit, that work is not rolled back, and how to give more.
- `steer` accepts `timeoutMs`: re-armed immediately on a running child, persisted for later segments. A budget already spent is refused rather than applied as an instant kill. The budget is per segment (`segmentStartedAt`), so revival gets a full one.
- Checks: 27 pass, including a real child carried past its original budget and a refused too-small one.

## Making the tooling legible to models (uncommitted)

Field reports, all from one incident: a forked child re-delegated its own brief, its `delegate` call failed because children have no delegation tools, it diagnosed that as an unloaded extension and asked for a reload, and ~$1.76 bought zero repo changes. The parent then could not tell the child's report from its own message in the returned text.

- **A fork no longer teaches delegation.** `stripDelegation` removes this package's own records from inherited context: `delegate`/`delegate_ctl` tool calls, their results, and completion notices. The parent's ordinary work — text, `read`/`bash` calls and results, other extensions' custom messages — is kept, and an assistant turn whose only content was a delegation call leaves no empty message.
- **The worker contract says it plainly.** The child is told it has no delegation tools, that its toolset is fixed rather than broken, never to ask for a reload or restart, and to ignore delegation patterns in inherited context.
- **Reports are fenced.** `----- <id> reported, verbatim -----` … `----- end of report -----` around the child's words, with a readable run line above it (`complete · id · role · model · context forked from N parent messages · 3 turns in 25s · tokens in 6, out 1.5k, cached 639.2k · $0.94`) instead of `[complete] … ↑6 ↓1.5k R639.2k`.
- **Revival can change offering.** `steer` takes `model:`; it applies from the next segment, persists for later ones, and is refused on a running child whose turn is already bound. An unavailable saved model now names the exact call to pick a replacement.
- **A named provider is never substituted.** `resolveModel` resolved `provider/id` to the same id on a different provider when the named one was missing — a silent route change across cost and limits. It now resolves the named provider only; bare ids still search.
- **Providers are mirrored per child session**, not once per process, so an extension that registers after the first child (account switch, reload) still reaches children; dropping one stops serving children that must reopen.
- Checks: 26 pass. Scoped mutation on `resolveModel` + `stripDelegation`: 71 mutants, 51 killed, 1 timeout, 19 survived (survivors are error wording and redundant guards).
- Confirmed on a real extension provider: `pi.registerProvider("vertex", …)` from pi-vertex is the only registered provider here, and a child launched on `vertex/glm-5.2` returned `complete` with its exact expected answer — 3 turns, $0.002, `openai-codex`-free. The report "extension providers are disabled in children" is fixed, not worked around.
  - **Corrected diagnosis.** That run took 10 minutes of wall clock and reported `3 turns`, and the first attempt on a 3 min budget timed out holding partial output (`VERTEX-`). The model is not slow: its transcript shows the stream emitting `VERTEX-` and then dying with `terminated` after ~5 minutes, twice, before a third attempt answered in **5 seconds with 32 output tokens**. Two stalled vertex streams and their retries were the whole 10 minutes. The earlier note here calling the offering "unusably slow" and prescribing a larger budget was wrong — a bigger budget on a stalling route only buys more stalls.
  - That misreading came from this package's own reporting: retried failures were counted as `turns`, and nothing named them. Now failed requests count as attempts, not turns; the report carries `N provider attempts failed and were retried before this (last: …)`; and a timeout whose budget went to failed attempts points at the provider instead of recommending more time.
  - **Chased and closed.** The endpoint accepts the request (`200 text/event-stream`) and then sends **zero bytes**, forever; undici gives up at its 300 s body timeout and reports `terminated`. Measured over raw fetch, no pi code involved: `zai-org/glm-5.2-maas` global stalled **9/19** (identical with and without tools), while `zai-org/glm-4.7-maas` (0/10) and `deepseek-ai/deepseek-v3.2-maas` (0/12) on the same endpoint, credentials and body never did. `glm-5.2` has no regional endpoint — `us-central1` returns HTTP 400. A bad Google deployment, not our code; it was behaving again by the end of the session (6/6 clean), so it is intermittent.
  - Client-side bound added in **pi-vertex 1.2.3**: the existing Vertex fetch wrapper now limits the wait for the *first* byte (default 90 s, `PI_VERTEX_FIRST_BYTE_TIMEOUT_MS`, `0` disables). Streams that have started are untouched, so slow-but-alive ones and mid-stream gaps behave as before. Covered by `pi-vertex/tests/first-byte-timeout-check.mjs` (`npm test`): dead stream fails fast, slow start survives, keepalives still stripped. Not reproduced live against the real stall — the endpoint stopped misbehaving before it could be caught.
  - Practical guidance: `vertex/glm-5.2` is not a dependable interactive child here. Prefer `glm-4.7` on the same route, or another offering.
  - Probes kept for the next occurrence: `/tmp/pi-vertex-stall/{probe.mjs,probe2.mjs,probe3.mjs,probe4.mjs,rate.mjs}` — `RUNS=8 MODEL=… LOCATION=… node rate.mjs` reports the stall rate for any MaaS model.
  - Harness used (kept out of the package, it imports absolute machine paths): `/tmp/pi-delegate-vertex-check/check.ts`, run from the package directory so `@earendil-works/*` resolves.

## Current uncommitted work: portable QC and native child detail

- `npm run check`: package-local strict TypeScript 5.9.3 and tsx/node:test on Pi SDK 0.86.1. **23 checks pass** without model credentials. Real HTTP loopback provider and SDK sessions cover reload, fork, wait/cancel/resume, idle/busy wake-up, reload-gap delivery, timeout/provider failure, failed persistence, cross-process ownership, cold inspection, and SIGKILL before delivery/before receipt persistence. The crash test waits the actual lease expiry. Direct tool-call fixtures seed durable parent receipts; automatic notification tests do not replace delivery.
- `npm run check:install`: candidate tarball, isolated dependency installation and real local-directory `pi install`; both tools, packaged skill, and roles load. npm/Git source overrides are runnable but were not exercised for this uncommitted candidate. Local paths need their dependencies installed first; Pi only links them.
- `npm run smoke:tui`: real regular/fullscreen Pi and actual built-in tools against the loopback provider. Native user/assistant/tool/editor components, collapsed/expanded results, Markdown/code rendering, partial bash output, newest-follow/pause/resume, 110×36 and 54×20, separate drafts, history, Ctrl+X stop and same-ID restart. Fullscreen native tool clicks and wheel scrolling pass; regular mode retains Pi's terminal-owned mouse behavior. Latest evidence: `/tmp/pi-delegate-qc-o34Axg/`.
- Fixed two reproduced defects: SDK provider error messages previously became successful completions; extension-registered providers appeared in the parent's catalog but were absent from the child's separate model runtime. Copy native/config provider registrations through the public registry APIs when constructing that runtime; never write provider credentials into child snapshots. Both paths have real-SDK regressions.
- Independent verification **done**, and it executed the checks rather than reading screenshots. A read-only reviewer ran `npm run check`, `npm run check:install`, `npm run smoke:tui`, and the scoped mutation command itself: all pass, **no P1/P2 defects**, one P3 note that the five mutation survivors are error wording and defensive conditions. It cited `src/transcript.ts` for the native components and collapsed-by-default tools, `src/inspector.ts` for draft separation and follow/pause, and `src/index.ts` for provider inheritance and the provider-failure check. Evidence: `.agents/pi/subsessions/2026-09-22T04-09-19-260Z_01a0c74d-ca9b-7353-a2df-315fcc44c2d7.jsonl`, 8 turns, $0.0102.
- Model route for that review: both Codex subscriptions returned `The usage limit has been reached`; the user then chose the same weights on the GitHub Copilot subscription. Approved defaults were **not** changed — `scout` still resolves to the originally approved offering. The exhausted-account failure is also live proof of both fixes: the offering resolved inside the child runtime (previously `Saved model is unavailable`) and the provider error surfaced as `error`, not `complete`.
- Reviving a child whose saved model cannot be reached still fails per segment with no fallback, by design. A reviewer launched against an exhausted subscription therefore needs a new offering chosen in conversation, not an automatic substitution.
- StrykerJS 10.0.0, command runner against the real lifecycle suite: baseline passes; **34 selected mutants: 21 killed, 8 timed out, 5 survived; zero uncovered/errors**. Scope: `src/index.ts:765–776,906–913` (provider registrations, preflight guard, provider failure). Survivors cover error wording/fallback and defensive guards not distinguished by these fixtures; this is not full mutation coverage or an independent review. Tooling/config/report remain in `/tmp/pi-delegate-mutation-tools/`, `/tmp/pi-delegate-stryker.config.mjs`, and `/tmp/pi-delegate-mutation.json` for the reviewer to execute. No runtime dependency or score gate added.
- README documents trigger-based checks. No new review orchestration, arbitrary score thresholds, or mandatory install/TUI checks for docs-only edits. No commit/push requested for this work yet.

## Verified

- [x] Fresh and fork context; fork proven by a child knowing a parent-only codeword with `startIdx` set past the replayed history.
- [x] `steer` on a finished child continues the same session (no new child, no new review); cumulative turns/tokens on one run id. It now returns immediately and runs in the background, just like launch. Reproduced the old blocking behavior (5.3 s, returned `complete`); the same real-child check now returns `running` in 14 ms. Verified attached wait suppresses a redundant wake, unjoined resume wakes and continues the parent exactly once, aborting the returned steer tool leaves the child running, and wait detach/explicit child cancel stay separate. Eight unit tests still pass. Evidence: `/tmp/pi-delegate-steer/{before,full}.jsonl`.
- [x] Background launch (the default) → `sendMessage(followUp, triggerTurn)` wake; `LAUNCHED` → `WOKEN` verified in RPC and in a real TUI. Fresh real SDK/child checks on Pi 0.86.1 passed with the parent idle, busy, and inside an awaited `agent_end` handler, both before and after the wait change. Exactly one notification per run. The reported missing-wake incident was **not reproduced**; no speculative wake-routing change was made.
- [x] `delegate_ctl wait` attaches to the current run segment without polling or relaunching. Attached waiters receive the terminal result instead of a redundant completion notification. Aborting a wait detaches only that waiter; explicit `cancel` stops the child. Late waits return the stored result; they do not retract an already-sent notification. Run IDs remain scoped to their parent. Live children now reconnect across reload; reopening the same parent restores saved children without executing them.
- [x] Eight completion unit tests pass (`npm test`). Live real-child checks passed for attached and late waits, same-session resume, waiter abort, actual parent abort during a wait, cancellation during startup and execution, timeout, setup error, unknown id, and synchronous launch. Aborted waits and parent abort both left the child running, with one normal notification on completion. A regular Pi TUI showed one `Wait TUI / WAIT-TUI` outcome followed by the parent's `JOINED`, with no duplicate completion card.
- [x] Reproduced a cancel/resume race: resuming before cancellation settled could replace the completion and strand an attached waiter. Resume now rejects while the previous segment is still stopping; retirement and writer exclusion also use settlement, not an early status change. The failing live reproduction now passes. Evidence: `/tmp/pi-delegate-completion/` (JSONL traces and `tui-proof.txt`).
- [x] Changed files from a git status/mtime delta per run segment, so `bash >>` edits are caught, not just `edit`/`write`.
- [x] One writer per cwd — a second `edit`/`write`-capable role in a live writer's tree is refused. `bash` does not count: read-only roles use it for grep/git diff/tests, and counting it collided two scouts (caught by the inspector test).
- [x] `models`: 967 offerings across 26 providers verbatim; live OpenRouter facts by exact id (current-window price vs registry, long-context tiers, UTC peak/off-peak windows with the active one marked, expiration, Artificial Analysis indices), per-provider endpoints for filtered candidates (discount, quantization, status, uptime, provider-specific off-peak). Spec: <https://openrouter.ai/docs/guides/overview/models#pricing-object>. Overrides with unrecognized condition fields are skipped and counted, per that spec.
- [x] `approve` persists a default; subsequent calls resolve it silently.
- [x] **Live-only navigation:** one pinned OMP-style Agents frame shows only running/stopping children; settled rows leave automatically and an empty frame disappears. Ctrl+J focuses active children or opens finished history when idle. `/agents` opens history even while other children run. No dismissal state; finished rows do not return after reload. Active rows keep dispatch order and selection, with four-row internal scrolling.
- [x] Enter opens a full-viewport child conversation, with actual streamed assistant text, tool arguments/results, and messages to the existing child. Esc returns to the parent editor; parent and child drafts remain separate. Ctrl+X stops only the displayed child. No separate rail, floating inspector, session-path injection, or live transcript card.
- [x] Async dispatch renders no transcript card. Completion renders one line per run segment: status glyph, task title, role, and elapsed time; Ctrl+O expands the complete report and metadata. Non-success statuses and unavailable-tool warnings remain visible when collapsed. Sync calls and attached waits return the outcome directly. Ordinary control-tool queries remain static transcript records.
- [x] Original navigation smoke (before the live-only cutover): five simultaneous real scouts in regular Pi; two in fullscreen Pi; list overflow, selection retained through completion, 110×36 and 54×20 terminals, selected-child steering (`CHILD-BRAVO`), same-session resume (`RESUMED-BRAVO`), cancellation, transcript scrolling, finished-row dismissal, parent draft restoration, and child draft restoration. A normal parent tool call also produced one `Render smoke / RENDER-OK` outcome without a dispatch card. The smoke driver suppressed parent model wake-ups, not child execution; automatic parent wake remains covered by the earlier verification above.
- [x] Independent read-only source review plus live tmux inspection found no concrete contract defects. UI files (`inspector.ts`, `render.ts`) pass TypeScript 5.9.3 against the installed Pi declarations.

### Live-only frame and on-demand history verification

- [x] Real Pi in regular and fullscreen modes: two live children shown together; the first completion removed only its row and preserved selection on the other child. Settling the last child removed the frame and restored parent-editor focus. Parent drafts survived history/child inspection.
- [x] One collapsed transcript line per completion, with the full report available via Ctrl+O. Success, cancellation, timeout, same-ID resumed work, and a real parent `delegate(sync: true)` call rendered correctly. No additional completion message for the joined call.
- [x] `/agents` opened finished history while another child ran. Idle Ctrl+J opened history; selecting a row opened the correct existing conversation without starting it. Explicit human messages revived the same child, including a stopped child after a fresh-process reopen. Active rows returned during revival and disappeared again on completion.
- [x] Finished rows stayed absent after `/reload` and cold reopen. History retained five children. Checked 110×36 and 54×20 terminals, narrow history and child views, and full-viewport child inspection.
- [x] Eleven existing tests and full-package strict TypeScript pass. Independent source/artifact review found no concrete defects. Evidence: `/tmp/pi-delegate-liveonly/` (TUI captures, trace, parent/child sessions, and verification driver). The UI driver disabled unrelated parent model wake-ups; it preserved real child execution and completion-message persistence/rendering. The synchronous case used an actual parent model tool call.

### Reload and revival verification

- [x] **In-process execution, replaceable extension binding.** Real Pi 0.86.1 `/reload` retained the same Run object, child session object, transcript path, and pending waiter; the original prompt appeared once. A child finishing during the detached reload interval produced one notification after reattachment. A second reload did not duplicate it.
- [x] **Crash-safe result recovery.** SIGKILL after child completion but before parent delivery recovered the report once without reopening the child. A second SIGKILL inside the parent's `message_end` hook—after in-memory acknowledgement but before Pi appended the receipt—also recovered the report once. Only the persisted parent receipt establishes durable delivery; an in-memory flag is not written as proof of delivery.
- [x] **Cold reopen is inspection, not execution.** A fresh process restored completed, interrupted, and explicitly stopped children. `status`, `result`, and `wait` left the child transcript size unchanged and created no child session. A SIGKILL during an actual bash tool restored as `interrupted`, not `running`, with no automatic restart.
- [x] **Original runtime contract.** Cold steer returned `running` in under 1 ms in the recorded check, then used the same ID/transcript/model/thinking/tools/timeout/role and project instructions. The child recalled both a persisted parent-only codeword and the original project marker after that instruction file changed. A parked child also revived from disk under the same ID. A partial-initialization replay with a system message but no user prompt preserved the original brief before the revival message.
- [x] **Sticky stops.** Ordinary steer refused an explicitly stopped child after cold reopen; `restart: true` resumed it. In a live TUI, opening a saved conversation read its full transcript; Ctrl+X stopped running work, the editor changed to Restart, and a direct human message resumed that child. All four children survived `/reload`; the later live-only UI puts finished children in history rather than pinning them.
- [x] **Cancellation during Pi preflight.** The first probe showed the SDK stream function was still entered after cancellation during prompt preparation. The dispatch-boundary guard now prevents entry: the same probe records zero stream calls and returns `cancelled` without waiting for a model response.
- [x] **Ownership and failure paths.** A second real Pi process opening the same parent was refused by the filesystem lease. Missing transcript and failed snapshot write both left the old completion and segment intact. Eleven tests pass (`npm test`), including atomic storage and exclusive leases; full-package strict TypeScript 5.9.3 checking now passes against installed Pi declarations.
- [x] Read-only independent source review found no concrete P1/P2 defect; live verification separately found and repaired the preflight cancellation race above. Evidence: `/tmp/pi-delegate-revive/` (JSONL traces and `tui-*.txt`).

Requires Pi 0.86.1+ for lifecycle reasons and the prompt dispatch callback. Model-runtime configuration refreshes with the extension binding; already-running children keep their existing runtime.

Implementation: process-global runtime registry, per-run ownership (`ownerPid`/`ownerHost`/`ownerToken`; the parent lease was dropped in `eea99c6`), atomic per-child JSON snapshots and one pointer file per run (the legacy parent index is still read). No detached runners, daemon, tmux dependency, or second session writer. Snapshot delivery is reconciled with receipts in the parent transcript. Recovery starts with children launched by this version; pre-upgrade JSONL files do not contain the required parent/runtime metadata.

## Fleet notes

- Machines still on pi-subagents: a newer pi-subagents rejects the removed `fallbackModels` field in `settings.subagents.agentOverrides.*`. Fix on the affected machine:
  `python3 -c "import json;p='$HOME/.pi/agent/settings.json'.replace('$HOME',__import__('os').path.expanduser('~'));d=json.load(open(p));[ov.pop('fallbackModels',None) for ov in d.get('subagents',{}).get('agentOverrides',{}).values()];json.dump(d,open(p,'w'),indent=2)"`
  Reproduced and verified here: error appears with the field, gone without it, both stacks then load together.
- Nothing under `~/.agents` or `~/.pi/agent` syncs between machines (no symlinks/git/syncthing found). This repo and playbook are the only shared vehicles; keep machine-local migration steps out of them.

## Open

- [ ] **Use it on real work.** The original baseline was 22 logged runs, 21 of them `/tmp` scratch; subsequent verification added scratch runs, not production evidence. Nothing here is proven on an actual multi-file change.
- [ ] If the original missing-wake incident recurs, capture the originating parent/child transcripts, Pi version, and any `send_message` extension error. Current local reproductions all resumed automatically; the original cause is still unknown.
- [ ] **Remote crash — local triage complete; repair blocked on remote evidence.** User confirmed `RangeError: Invalid string length` in Node readline occurred on another machine. Need that failed session's last tool call and Pi version before attributing/fixing it. Local Pi 0.86.1 `dist/core/tools/grep.js:102-145` sends unbounded ripgrep JSON through readline before truncating output. A bounded 8 MiB-line probe produced an 8,388,813-byte readline record but only 602 returned bytes; it did not crash. This proves late truncation does not bound input buffering, not that grep caused the remote incident. `find` also uses readline on a subprocess pipe. Evidence: `/tmp/pi-delegate-steer/grep-proof.json`. No speculative runtime patch applied.
- [ ] Approved defaults exist only for `scout`. `worker`, `reviewer`, `ui-verifier`, `ui-panelist` need a proposal → agreement → `approve` each.
- [ ] `catalogAtApproval` for `scout` is empty (file was hand-written); the next real `approve` populates it and "new since approval" drift starts working.
- [ ] Ratings cache empty. AA arrives live, so this is only for evidence beyond AA (observed runs here, other evals, an offering's serving quality).

## Known gaps, deferred on purpose

- **Children get built-in tools only.** Not fixable from an extension: `pi.getAllTools()` returns `ToolInfo` (name/description/parameters, no `execute`). Options if a research/web role is ever needed: role frontmatter naming extension paths for the child loader (unverified against `noExtensions: true`), or an upstream request for `ToolDefinition` access. `droppedTools` reports the loss on every run, so it never fails silently.
- **No execution through process death.** Reload preserves live children, but quit/session replacement interrupts them. Cold recovery requires an explicit message to resume. A crashed process can leave external tool side effects; inspect before revival. A stale ownership lease expires within 10 seconds. This is not a detached-worker system.
- No missions, lanes, watchdogs, acceptance protocols, councils, schedules, or intercom. A brief, a slash command, or `git worktree add` covers those; none belong in a schema read every turn.

## Metrics to move

Baseline from the old stack; re-audit at 20 real (non-scratch) runs in `delegate-runs.jsonl`.

| metric | baseline | target | now |
|---|---|---|---|
| review runs per implementation run | 1.26 | <0.5 | — |
| failed runs | 34% | <10% | 0/22 (scratch) |
| delegation tools in context | 11 | 2 | 2 |
| schema params | ~125 | ~10 | 10 |
| median child wall-clock per session | 22 min | <8 min | — |

Guardrail: rework (steers per run) must not rise as review falls.

## Related

- Skill: ships in the package (`skills/delegation/SKILL.md`) — when to delegate, review triggers, model research and proposal procedure. Install via `pi install npm:@ssweens/pi-delegate | git:… | ./dir`; extensions, skill, and roles all travel together. No copies in `~/.agents`.
- Method that produced this design: `friction-audit` skill (`~/src/playbook/skills/friction-audit/`).
- Retired: `~/.agents/agents/_retired-stage0/` (dead Claude-format roles, sol-advisor trio), `~/.pi/agent/_retired-stage0/` (missions, pi-strings state). `run-history.jsonl` kept as the re-audit baseline.
