---
phase: 11-import-command-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/import/types.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/orchestrators/import/index.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - tests/orchestrators/import/execute.test.ts
autonomous: true
requirements: [IMP-09, IMP-10, IMP-11]
requirements_addressed: [IMP-09, IMP-10, IMP-11]
must_haves:
  truths:
    - "Import executes planned marketplace ensures before dependent plugin installs per scope."
    - "Already-present matching marketplaces and already-installed plugins are silent idempotent skips, with all-skipped runs reported as already up to date."
    - "Marketplace add failure skips only plugins depending on that marketplace; unrelated imports continue."
    - "Plugin install outcomes are classified into installed, skipped, warning, and unexpected affected-plugin failure without aborting unrelated imports."
    - "Reload guidance is aggregated once at import end by adding a notification-control seam to installPlugin rather than duplicating install logic."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
      provides: "Phase 11 import execution orchestrator"
    - path: "tests/orchestrators/import/execute.test.ts"
      provides: "IMP-09/IMP-10/IMP-11 orchestration coverage"
---

<objective>
Implement the import execution orchestrator that consumes Phase 10 plans and delegates mutations to existing marketplace-add and plugin-install semantics.
</objective>

<context>
@.planning/phases/11-import-command-orchestration/11-CONTEXT.md
@.planning/phases/11-import-command-orchestration/11-RESEARCH.md
@.planning/phases/11-import-command-orchestration/11-VALIDATION.md
@extensions/pi-claude-marketplace/orchestrators/import/{types,settings,refs,marketplaces,index}.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add import execution result types and summary formatter</name>
  <files>extensions/pi-claude-marketplace/orchestrators/import/types.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts, extensions/pi-claude-marketplace/orchestrators/import/index.ts, tests/orchestrators/import/execute.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/import/types.ts; extensions/pi-claude-marketplace/orchestrators/import/marketplaces.ts; extensions/pi-claude-marketplace/shared/notify.ts; extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts</read_first>
  <action>Create `orchestrators/import/execute.ts`. Define import execution outcome types carrying `scope`, `plugin@marketplace`, `marketplace`, `reason`, and optional `cause`. Include buckets for added marketplaces, installed plugins, skipped existing items, unavailable/uninstallable warnings, marketplace failures, source mismatches, unexpected plugin failures, and carried Phase 10 diagnostics. Add a pure summary formatter used by the orchestrator that groups outcomes by scope and emits an "already up to date" message when no changes were made and all actions were idempotent skips.</action>
  <verify><automated>npm test -- tests/orchestrators/import/execute.test.ts</automated></verify>
  <acceptance_criteria>
    - `rg "already up to date" extensions/pi-claude-marketplace/orchestrators/import/execute.ts tests/orchestrators/import/execute.test.ts` finds formatter coverage.
    - `rg "plugin@marketplace|scope|reason|cause" tests/orchestrators/import/execute.test.ts` finds assertions that warning records carry actionable fields.
    - `npm test -- tests/orchestrators/import/execute.test.ts` exits 0 for pure formatter tests.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add installPlugin notification-control and typed outcome seam</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/orchestrators/import/execute.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts; extensions/pi-claude-marketplace/shared/errors.ts; extensions/pi-claude-marketplace/presentation/reload-hint.ts; tests/orchestrators/plugin/install.test.ts</read_first>
  <action>Extend `InstallPluginOptions` with an optional import-safe notification mode, e.g. `notifications?: { reloadHint?: "immediate" | "suppress"; returnOutcome?: true }`, or a similarly typed option. Preserve current default behavior exactly. When used by import, suppress only the immediate per-plugin reload hint and return a typed outcome that distinguishes success/resource-changed, already-installed, unavailable/not-found, uninstallable, and unexpected failure with preserved cause text. Do not duplicate install staging or resolver logic outside `installPlugin`.</action>
  <verify><automated>npm test -- tests/orchestrators/import/execute.test.ts tests/orchestrators/plugin/install.test.ts</automated></verify>
  <acceptance_criteria>
    - `rg "reloadHint.*suppress|suppress.*reloadHint|returnOutcome" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` finds the new seam.
    - Existing plugin install tests still assert default success messages include `Run /reload` when resources changed.
    - New tests assert import-mode install does not emit an immediate `Run /reload` notification but reports enough outcome data for import to aggregate one reload hint.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute per-scope import plans with idempotency and source mismatch checks</name>
  <files>extensions/pi-claude-marketplace/orchestrators/import/execute.ts, tests/orchestrators/import/execute.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/import/marketplaces.ts; extensions/pi-claude-marketplace/orchestrators/import/settings.ts; extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts; extensions/pi-claude-marketplace/domain/source.ts; extensions/pi-claude-marketplace/persistence/state-io.ts</read_first>
  <action>Implement `importClaudeSettings(opts)` or equivalent. Options must include `ctx`, `pi`, `cwd`, selected scopes, and injectable `gitOps`. For each selected scope, load merged settings, build the Phase 10 plan, read state for preflight, skip existing matching marketplace records, detect existing source mismatch by parsing the planned source and comparing with stored normalized source, call `addMarketplace` for missing marketplaces, then install plugins whose marketplace was ensured and not mismatched/failed. Keep scopes independent; do not introduce a Claude `local` scope.</action>
  <verify><automated>npm test -- tests/orchestrators/import/execute.test.ts</automated></verify>
  <acceptance_criteria>
    - Test assertion: existing marketplace with matching source is not passed to `addMarketplace` and records a silent skip.
    - Test assertion: existing marketplace with different source records `source-mismatch` and skips dependent plugins without calling `installPlugin` for them.
    - Test assertion: marketplace add failure for `mp-a` skips only `*@mp-a`; `*@mp-b` still installs.
    - Test assertion: same plugin enabled in user and project creates independent user/project operations.
    - `rg "local" extensions/pi-claude-marketplace/orchestrators/import/execute.ts` does not reveal a new scope branch.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Aggregate warnings/errors and final reload guidance once</name>
  <files>extensions/pi-claude-marketplace/orchestrators/import/execute.ts, tests/orchestrators/import/execute.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/presentation/reload-hint.ts; extensions/pi-claude-marketplace/presentation/soft-dep.ts; extensions/pi-claude-marketplace/shared/notify.ts; .planning/phases/11-import-command-orchestration/11-CONTEXT.md decisions D-03 through D-07</read_first>
  <action>Finish notification behavior in `importClaudeSettings`: carry Phase 10 diagnostics, marketplace failures, source mismatches, unavailable/uninstallable install outcomes, already-installed skips, and unexpected affected-plugin failures into one final summary. Include `plugin@marketplace`, target `scope`, `reason`, and preserved cause text for warnings/failures. Append one reload hint at the end only if at least one plugin install changed resources. Use `notifySuccess` for clean success/up-to-date and `notifyWarning` when warnings/skips/failures were aggregated.</action>
  <verify><automated>npm test -- tests/orchestrators/import/execute.test.ts && npm run typecheck</automated></verify>
  <acceptance_criteria>
    - Test assertion: unavailable plugin produces warning summary and does not abort unrelated plugin install.
    - Test assertion: unexpected plugin failure includes preserved cause text and does not abort unrelated plugin install.
    - Test assertion: final notifications contain at most one `Run /reload` line for a mixed import.
    - Test assertion: all-skipped import reports `already up to date`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `npm test -- tests/orchestrators/import/execute.test.ts tests/orchestrators/import/settings.test.ts tests/orchestrators/import/refs.test.ts tests/orchestrators/import/marketplaces.test.ts`, then `npm run typecheck`.
</verification>

<success_criteria>
The import orchestrator safely executes Phase 10 plans with idempotency, source-mismatch protection, classified plugin outcomes, per-scope isolation, and a single aggregated user-facing summary.
</success_criteria>

<output>
After completion, create `.planning/phases/11-import-command-orchestration/11-01-SUMMARY.md`.
</output>
