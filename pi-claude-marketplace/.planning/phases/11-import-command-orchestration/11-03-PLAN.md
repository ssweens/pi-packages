---
phase: 11-import-command-orchestration
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - tests/e2e/import-command.test.ts
  - tests/fixtures/import-command/README.md
  - tests/fixtures/import-command/user-claude/settings.json
  - tests/fixtures/import-command/user-claude/settings.local.json
  - tests/fixtures/import-command/project/.claude/settings.json
  - tests/fixtures/import-command/project/.claude/settings.local.json
  - tests/fixtures/import-command/directory-marketplace/.claude-plugin/marketplace.json
  - tests/fixtures/import-command/directory-marketplace/plugins/local-plugin/plugin.json
  - tests/fixtures/import-command/github-marketplace/.claude-plugin/marketplace.json
  - tests/fixtures/import-command/github-marketplace/plugins/github-plugin/plugin.json
  - .planning/phases/11-import-command-orchestration/11-VALIDATION.md
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03, IMP-09, IMP-10, IMP-11]
requirements_addressed: [IMP-01, IMP-02, IMP-03, IMP-09, IMP-10, IMP-11]
must_haves:
  truths:
    - "A rich command-level fixture exercises the import command as a user runs it."
    - "Fixture covers official GitHub, extra-known directory, extra-known GitHub, local override disabling a base plugin, already-installed skip, unavailable warning, both scopes, final summary, and source mismatch."
    - "Targeted tests and `npm run check` pass before Phase 11 sign-off."
  artifacts:
    - path: "tests/e2e/import-command.test.ts"
      provides: "End-to-end Phase 11 import command validation"
    - path: ".planning/phases/11-import-command-orchestration/11-VALIDATION.md"
      provides: "Final validation evidence map updated with implemented command names"
---

<objective>
Add end-to-end import command fixtures and close Phase 11 with validation evidence.
</objective>

<context>
@.planning/phases/11-import-command-orchestration/11-CONTEXT.md
@.planning/phases/11-import-command-orchestration/11-RESEARCH.md
@.planning/phases/11-import-command-orchestration/11-VALIDATION.md
@extensions/pi-claude-marketplace/edge/register.ts
@extensions/pi-claude-marketplace/edge/router.ts
@extensions/pi-claude-marketplace/orchestrators/import/execute.ts
@tests/e2e
@tests/helpers
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build rich import command fixtures</name>
  <files>tests/fixtures/import-command/**, tests/e2e/import-command.test.ts</files>
  <read_first>tests/orchestrators/marketplace/add.test.ts; tests/orchestrators/plugin/install.test.ts; tests/orchestrators/import/marketplaces.test.ts; tests/helpers/git-mock.ts; .planning/phases/11-import-command-orchestration/11-CONTEXT.md decisions D-11 and D-12</read_first>
  <action>Create a fixture tree under `tests/fixtures/import-command/` containing Claude user settings and project `.claude/settings*.json`, a path-source marketplace, and a fake GitHub marketplace served by existing `gitOps` test helpers. Include enabled refs for `plugin@claude-plugins-official`, one directory marketplace plugin, one extra-known GitHub marketplace plugin, one base-enabled plugin disabled by local settings, one unavailable plugin, and one plugin that will be preinstalled before the import run. Keep fixture plugins minimal but installable by existing resolver/bridge code.</action>
  <verify><automated>npm test -- tests/e2e/import-command.test.ts</automated></verify>
  <acceptance_criteria>
    - `find tests/fixtures/import-command -name 'settings.json' -o -name 'settings.local.json'` shows both base and local settings for user and project fixture scopes.
    - `rg "claude-plugins-official|directory|github|false|unavailable" tests/fixtures/import-command tests/e2e/import-command.test.ts` finds fixture coverage for required cases.
    - Test setup preinstalls one plugin before invoking import so already-installed skip is observed.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add command-level import e2e assertions</name>
  <files>tests/e2e/import-command.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/edge/register.ts; extensions/pi-claude-marketplace/edge/router.ts; tests/e2e/*.test.ts; tests/helpers/*.ts</read_first>
  <action>Implement `tests/e2e/import-command.test.ts` using `node:test`. Invoke the registered `/claude:plugin import` command or `routeClaudePlugin` through the same handler map shape used by registration. Use isolated `HOME`, `CLAUDE_CONFIG_DIR`, and project cwd. Assert final Pi state contains imported marketplaces/plugins in the expected user and project scopes, the locally disabled base plugin is absent, unavailable plugin is reported as a warning, and final summary includes both scopes. Also include a source-mismatch scenario where an existing marketplace name has a different source and dependent imports are skipped.</action>
  <verify><automated>npm test -- tests/e2e/import-command.test.ts</automated></verify>
  <acceptance_criteria>
    - Test assertion: `/claude:plugin import` with omitted scope imports both user and project fixture scopes.
    - Test assertion: `/claude:plugin import --scope project` writes only project scope in a narrowed run.
    - Test assertion: final notifications include a summary and at most one `Run /reload` line.
    - Test assertion: source mismatch notification names the marketplace, dependent `plugin@marketplace`, scope, and reason.
    - Test assertion: disabled-by-local plugin does not appear in state or staged resources.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final targeted and full validation sign-off</name>
  <files>.planning/phases/11-import-command-orchestration/11-VALIDATION.md</files>
  <read_first>.planning/phases/11-import-command-orchestration/11-VALIDATION.md; package.json; tests/orchestrators/import; tests/edge; tests/e2e/import-command.test.ts</read_first>
  <action>Run all Phase 11 targeted tests plus `npm run check`. Update `11-VALIDATION.md` with the exact implemented targeted commands if filenames changed during execution and record any notable validation caveats. Do not mark sign-off complete unless the full gate is green.</action>
  <verify><automated>npm test -- tests/orchestrators/import/execute.test.ts tests/edge/handlers/import.test.ts tests/edge/router.test.ts tests/edge/completions/provider.test.ts tests/edge/register.test.ts tests/e2e/import-command.test.ts && npm run check</automated></verify>
  <acceptance_criteria>
    - CLI assertion: targeted Phase 11 test command exits 0.
    - CLI assertion: `npm run check` exits 0.
    - `rg "IMP-01|IMP-02|IMP-03|IMP-09|IMP-10|IMP-11" .planning/phases/11-import-command-orchestration/11-0*-PLAN.md` finds every Phase 11 requirement ID.
    - `rg "process\.stdout|process\.stderr|console\.log|console\.error" extensions/pi-claude-marketplace/orchestrators/import extensions/pi-claude-marketplace/edge` does not find new forbidden output-channel usage.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the targeted Phase 11 test command from `11-VALIDATION.md`, then `npm run check`.
</verification>

<success_criteria>
Phase 11 has a command-level fixture proving `/claude:plugin import` works end-to-end for both scopes with idempotency, warning aggregation, source mismatch protection, and final summary output.
</success_criteria>

<output>
After completion, create `.planning/phases/11-import-command-orchestration/11-03-SUMMARY.md` and mark Phase 11 validation complete.
</output>
