---
phase: 07-integration-pi-wiring
plan: 06
type: execute
wave: 5
depends_on: [07-02, 07-04, 07-05]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - CHANGELOG.md
  - .planning/phases/07-integration-pi-wiring/07-VALIDATION.md
autonomous: true
requirements: [NFR-2, NFR-3, NFR-8, NFR-11]
must_haves:
  truths:
    - "Phase 7 user-contract changes are recorded in planning docs."
    - "Validation strategy reflects implemented Wave 0 tests and phase gates."
    - "NFR-2, NFR-3, NFR-8, and NFR-11 are traceable from requirements to automated checks."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PI-15 supersession and Phase 7 traceability"
    - path: ".planning/PROJECT.md"
      provides: "D-25 key decision row"
    - path: "CHANGELOG.md"
      provides: "User-visible lock marker contract change note"
    - path: ".planning/phases/07-integration-pi-wiring/07-VALIDATION.md"
      provides: "Validation sign-off updates"
  key_links:
    - from: "Phase 7 D-08"
      to: ".planning/REQUIREMENTS.md PI-15"
      via: "supersession note"
      pattern: "superseded by Phase 7 D-08"
---

<objective>
Close Phase 7 planning-document traceability and validation sign-off.

Purpose: D-08 deliberately changes the user-visible concurrent-install marker; the project docs must record the contract change and validation artifacts must map every Phase 7 requirement to a green automated gate.
Output: REQUIREMENTS/PROJECT/CHANGELOG updates and VALIDATION sign-off.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-integration-pi-wiring/07-CONTEXT.md
@.planning/phases/07-integration-pi-wiring/07-VALIDATION.md
@.planning/phases/07-integration-pi-wiring/07-RESEARCH.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record PI-15 supersession and D-25 project decision per D-08</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, CHANGELOG.md</files>
  <read_first>.planning/REQUIREMENTS.md; .planning/PROJECT.md; CHANGELOG.md; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 60-68 and 253-255; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 34-40 and 398-402</read_first>
  <action>Update `.planning/REQUIREMENTS.md` so PI-15 is struck through and annotated as superseded by Phase 7 D-08: the loser now fails at lock acquisition with `STATE_LOCK_HELD_PREFIX` instead of reaching the old `was installed concurrently` state-guard commit path. Add a `.planning/PROJECT.md` Key Decisions row `D-25` describing the Phase 7 lock marker supersession and rationale. Add a `CHANGELOG.md` entry noting the user-visible concurrent-operation message prefix `Another pi-claude-marketplace operation is in progress for` and that retry remains the recovery action. Preserve the existing supersession style from D-21, D-23, and D-24.</action>
  <verify>
    <automated>npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `REQUIREMENTS.md` PI-15 contains `superseded by Phase 7 D-08`.
    - Source assertion: `PROJECT.md` Key Decisions contains `D-25` and `STATE_LOCK_HELD_PREFIX` rationale.
    - Source assertion: `CHANGELOG.md` mentions `Another pi-claude-marketplace operation is in progress for`.
  </acceptance_criteria>
  <done>Supersession trail is documented across requirements, project decisions, and changelog.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 7 validation sign-off and traceability</name>
  <files>.planning/phases/07-integration-pi-wiring/07-VALIDATION.md</files>
  <read_first>.planning/phases/07-integration-pi-wiring/07-VALIDATION.md; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 439-471; .planning/ROADMAP.md lines 159-167</read_first>
  <action>Update `07-VALIDATION.md` after Plans 07-02 through 07-05 land. Set `nyquist_compliant: true` and `wave_0_complete: true` only when `tests/orchestrators/discover.test.ts`, `tests/architecture/manifest-read-seam.test.ts`, `tests/integration/concurrent-install.test.ts`, e2e fixture files, `tests/e2e/pi-runtime-smoke.test.ts`, package scripts, and workflows exist. Fill `Plan` values in the Per-Task Verification Map: NFR-2 in Plan 07-05, NFR-3 in Plan 07-04, NFR-8 in Plan 07-02, NFR-11 in Plan 07-01 and Plan 07-05. Replace `TBD` feedback latency values with measured or observed command runtimes from executor output. Mark validation sign-off approval as complete only after the full phase gate passes and the real Pi-runtime smoke is green; if subprocess automation was proven nonviable, require the blocking manual Pi-runtime smoke evidence from Plan 07-05 before sign-off.</action>
  <verify>
    <automated>npm run check && npm run test:integration && npm run test:e2e && npm pack --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `nyquist_compliant: true` and `wave_0_complete: true` are present only after referenced files exist.
    - Source assertion: verification map references Plans `07-01`, `07-02`, `07-04`, and `07-05` for NFR-11, NFR-8, NFR-3, and NFR-2 respectively.
    - CLI assertion: full phase gate command passes or documents only classified upstream-change nightly behavior outside PR mode.
    - Source assertion: validation sign-off is not marked complete while `tests/e2e/pi-runtime-smoke.test.ts` is absent/failing or the documented manual fallback gate is unresolved.
  </acceptance_criteria>
  <done>Validation strategy reflects implemented tests and full phase gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Implementation -> project record | User-contract changes must be auditable in requirements and decisions. |
| Test results -> validation sign-off | Automated gates determine readiness to verify Phase 7. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Repudiation | PI-15 supersession docs | mitigate | Record D-08 in REQUIREMENTS, PROJECT D-25, and CHANGELOG with exact marker string. |
| T-07-03 | Repudiation | validation status | mitigate | Require the full phase gate before marking validation sign-off complete. |
</threat_model>

<verification>
Run `npm run check && npm run test:integration && npm run test:e2e && npm pack --dry-run`.
</verification>

<success_criteria>
Every Phase 7 requirement and user-contract change is documented and tied to an automated verification command.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-pi-wiring/07-06-SUMMARY.md`.
</output>
