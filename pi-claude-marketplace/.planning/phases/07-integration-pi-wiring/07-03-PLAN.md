---
phase: 07-integration-pi-wiring
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/discover.ts
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/index.ts
  - tests/orchestrators/discover.test.ts
  - tests/shared/index-smoke.test.ts
autonomous: true
requirements: [NFR-2, NFR-11]
must_haves:
  truths:
    - "A real Pi load registers `/claude:plugin`, read-only marketplace tools, and `resources_discover`."
    - "`/reload` can discover staged skills and prompts without Pi restart."
  artifacts:
    - path: "extensions/pi-claude-marketplace/index.ts"
      provides: "Real Pi extension entrypoint"
    - path: "extensions/pi-claude-marketplace/orchestrators/discover.ts"
      provides: "Disk-backed resources_discover aggregator"
    - path: "tests/orchestrators/discover.test.ts"
      provides: "SK-5 / NFR-2 discovery coverage"
  key_links:
    - from: "index.ts"
      to: "orchestrators/discover.ts"
      via: "pi.on resources_discover callback"
      pattern: "pi\.on\(\"resources_discover\""
---

<objective>
Replace the extension stub with real Pi wiring and disk-backed `resources_discover`.

Purpose: Phase 7's main user outcome is that Pi can load the extension, register the command/tools/events, and surface installed skills/prompts after `/reload`.
Output: `aggregateDiscoveredResources`, `AggregateResourcesDiscoverError`, real `index.ts`, and tests.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-integration-pi-wiring/07-CONTEXT.md
@.planning/phases/07-integration-pi-wiring/07-RESEARCH.md
@.planning/phases/07-integration-pi-wiring/07-PATTERNS.md
@extensions/pi-claude-marketplace/index.ts
@extensions/pi-claude-marketplace/edge/register.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@tests/edge/register.test.ts

<interfaces>
`aggregateDiscoveredResources(userLocations, projectLocations): Promise<{ skillPaths: string[]; promptPaths: string[] }>` walks `locations.skillsTargetDir` and `locations.promptsTargetDir` for both scopes. `AggregateResourcesDiscoverError` carries per-scope/per-kind failures and uses `Error.cause` for the aggregate chain.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement disk-backed resources_discover aggregator per D-10 through D-12</name>
  <files>extensions/pi-claude-marketplace/orchestrators/discover.ts, extensions/pi-claude-marketplace/shared/errors.ts, tests/orchestrators/discover.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/skills/discover.ts; extensions/pi-claude-marketplace/bridges/commands/discover.ts; extensions/pi-claude-marketplace/persistence/locations.ts; extensions/pi-claude-marketplace/shared/errors.ts; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 53-59; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 191-252 and 386-425</read_first>
  <behavior>
    - Missing `resources/skills` or `resources/prompts` directory returns an empty list for that kind.
    - Skill discovery returns sorted skill directory paths under both scopes; prompt discovery returns sorted `.md` file paths under both scopes.
    - ENOENT and ENOTDIR are silent empty cases; EACCES, EIO, ELOOP, and other fs errors are collected and thrown as `AggregateResourcesDiscoverError` after both scopes are attempted.
    - User and project same-name resources are both returned; no dedup occurs per delegated D-10 choice.
  </behavior>
  <action>Create `orchestrators/discover.ts` with `aggregateDiscoveredResources(userLocations, projectLocations)`. Walk `skillsTargetDir` for directories containing staged skills and `promptsTargetDir` for flat `*.md` prompts. Sort paths for stable output. Add `AggregateResourcesDiscoverError` to `shared/errors.ts` with a readonly failures array containing scope, kind, path, and cause. Write `tests/orchestrators/discover.test.ts` covering missing dirs, empty dirs, staged resources, mixed user/project output, non-md prompt filtering, no dedup, and aggregate throw after attempting both scopes. Use chmod/error cases with platform guards if macOS permission semantics require them.</action>
  <verify>
    <automated>node --test tests/orchestrators/discover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `node --test tests/orchestrators/discover.test.ts` passes.
    - Source assertion: `orchestrators/discover.ts` does not import `state-io.ts` or read `state.json`.
    - Behavior assertion: test fixture with both user and project resources returns both paths in deterministic order.
  </acceptance_criteria>
  <done>`resources_discover` has a pure disk-backed aggregator with PRD SK-5 error semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace index.ts stub with real Pi registration per D-10 and D-13</name>
  <files>extensions/pi-claude-marketplace/index.ts, tests/shared/index-smoke.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/index.ts; extensions/pi-claude-marketplace/edge/register.ts; extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts; extensions/pi-claude-marketplace/orchestrators/plugin/update.ts; extensions/pi-claude-marketplace/persistence/locations.ts; extensions/pi-claude-marketplace/platform/pi-api.ts; tests/shared/index-smoke.test.ts; tests/edge/register.test.ts; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 13-18 and 211-218; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 331-359</read_first>
  <behavior>
    - Calling the default export with a mock `ExtensionAPI` registers exactly one `resources_discover` handler.
    - The same call registers `/claude:plugin` and the two read-only marketplace tools through Phase 6 helpers.
    - The `resources_discover` callback resolves `locationsFor("user", homedir())` and `locationsFor("project", process.cwd())` at invocation time, not module import time.
  </behavior>
  <action>Replace the Phase 1 stub in `index.ts` with the real entrypoint. Import `homedir` from `node:os`, `locationsFor`, `aggregateDiscoveredResources`, `DEFAULT_GIT_OPS`, `updateSinglePlugin`, `registerClaudePluginCommand`, and `registerClaudeMarketplaceTools`. Register `pi.on("resources_discover", async () => aggregateDiscoveredResources(locationsFor("user", homedir()), locationsFor("project", process.cwd())))` per D-13. Then call `registerClaudePluginCommand(pi, { gitOps: DEFAULT_GIT_OPS, pluginUpdate: updateSinglePlugin })` and `registerClaudeMarketplaceTools(pi)`. Update `tests/shared/index-smoke.test.ts` from the Phase 1 stub expectations to assert real command/tool/event registration.</action>
  <verify>
    <automated>node --test tests/shared/index-smoke.test.ts tests/edge/register.test.ts tests/orchestrators/discover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `index.ts` no longer imports `notifyWarning` and no longer returns empty `skillPaths` / `promptPaths` unconditionally.
    - Source assertion: `index.ts` imports `ExtensionAPI` from `./platform/pi-api.ts`.
    - Behavior assertion: mock Pi sees command `claude:plugin`, two LLM tools, and one `resources_discover` handler.
  </acceptance_criteria>
  <done>The extension loads as a real Pi entrypoint and wires Phase 6 surfaces plus discovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> Pi resources_discover | Staged resource paths are reported to Pi after `/reload`. |
| Pi runtime -> extension entrypoint | Pi calls command/tool/event registration hooks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | discovered resource paths | mitigate | Discover only from `ScopedLocations.skillsTargetDir` and `promptsTargetDir`; no user-supplied paths are accepted. |
| T-07-04 | Information Disclosure | resources_discover smoke | mitigate | Tests assert path presence only; they do not invoke skill/prompt bodies or LLM turns. |
</threat_model>

<verification>
Run `node --test tests/shared/index-smoke.test.ts tests/orchestrators/discover.test.ts tests/edge/register.test.ts`.
</verification>

<success_criteria>
Pi can load the extension, `/reload` discovery is disk-backed, and command/tool registration flows through existing Phase 6 helpers.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-pi-wiring/07-03-SUMMARY.md`.
</output>
