---
phase: 07-integration-pi-wiring
plan: 05
type: execute
wave: 4
depends_on: [07-03, 07-04]
files_modified:
  - package.json
  - tests/e2e/_pinned-sha.ts
  - tests/e2e/_targets.ts
  - tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/marketplace.json
  - tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/plugins/frontend-design/plugin.json
  - tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/plugins/code-review/plugin.json
  - tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/plugins/code-simplifier/plugin.json
  - tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/plugins/context7/plugin.json
  - tests/e2e/resources-discover.test.ts
  - tests/e2e/install-soft-deps.test.ts
  - tests/e2e/pi-runtime-smoke.test.ts
  - tests/e2e/nightly-classification.test.ts
  - .github/workflows/ci.yml
  - .github/workflows/e2e-nightly.yml
autonomous: true
requirements: [NFR-2, NFR-3, NFR-11]
must_haves:
  truths:
    - "Pinned-SHA PR e2e verifies `/reload` surfaces resources without Pi restart."
    - "A real Pi-runtime smoke loads the extension in a subprocess with isolated HOME before validation sign-off."
    - "Nightly floating-main e2e classifies upstream drift separately from regressions."
    - "CI runs unit, integration, e2e, and package dry-run gates without watch mode."
  artifacts:
    - path: "tests/e2e/_pinned-sha.ts"
      provides: "Auditable upstream SHA pin"
    - path: "tests/e2e/_targets.ts"
      provides: "Four representative plugin targets and rationale"
    - path: ".github/workflows/e2e-nightly.yml"
      provides: "Floating-main nightly e2e workflow"
  key_links:
    - from: "tests/e2e/resources-discover.test.ts"
      to: "extensions/pi-claude-marketplace/index.ts"
      via: "mock ExtensionAPI registration and resources_discover event"
      pattern: "resources_discover"
    - from: "tests/e2e/pi-runtime-smoke.test.ts"
      to: "extensions/pi-claude-marketplace/index.ts"
      via: "subprocess Pi runtime load with isolated HOME/cwd"
      pattern: "HOME"
---

<objective>
Build the Phase 7 live e2e and CI validation layer.

Purpose: The phase must prove the integrated extension works against `anthropics/claude-plugins-official` with deterministic PR coverage and floating-main drift classification.
Output: e2e fixtures/tests/scripts and CI/nightly workflows.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-integration-pi-wiring/07-CONTEXT.md
@.planning/phases/07-integration-pi-wiring/07-RESEARCH.md
@.planning/phases/07-integration-pi-wiring/07-VALIDATION.md
@.planning/phases/07-integration-pi-wiring/07-PATTERNS.md
@package.json
@.github/workflows/ci.yml
@tests/edge/register.test.ts
@extensions/pi-claude-marketplace/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pinned e2e fixtures and target catalog per D-01a through D-01c</name>
  <files>tests/e2e/_pinned-sha.ts, tests/e2e/_targets.ts, tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/marketplace.json, tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/plugins/frontend-design/plugin.json, tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/plugins/code-review/plugin.json, tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/plugins/code-simplifier/plugin.json, tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/plugins/context7/plugin.json</files>
  <read_first>.planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 38-45 and 225-245; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 256-270 and 439-471; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 428-484</read_first>
  <behavior>
    - `PINNED_SHA` equals `6196a61bdeece7b9889ecda1e45bd7085788ae75`.
    - `TARGETS` contains `frontend-design`, `code-review`, `code-simplifier`, and `context7` with kinds `skills`, `commands`, `agents`, and `mcp` respectively, and each target can resolve to an installable local source path inside a cloned pinned upstream checkout.
    - Agents-only and MCP-only targets have `softDepMatrix: true`; skills-only and commands-only targets have `softDepMatrix: false`.
  </behavior>
  <action>Create the e2e fixture scaffold. Store `PINNED_SHA` as a code constant with a comment saying the pin is refreshed by manual PR. Create `_targets.ts` with `E2ETarget` and four static targets from research: `frontend-design` for skills, `code-review` for commands, `code-simplifier` for agents, and `context7` for MCP; include a rationale string for each and enough relative-path metadata for tests to locate each plugin source in an upstream checkout. Add fixture snapshot files under `tests/e2e/_fixtures/6196a61bdeece7b9889ecda1e45bd7085788ae75/`: `marketplace.json`, plus `plugins/frontend-design/plugin.json`, `plugins/code-review/plugin.json`, `plugins/code-simplifier/plugin.json`, and `plugins/context7/plugin.json`. These snapshots are for deterministic manifest/plugin metadata and nightly diff classification. Do not treat those manifest snapshots as installable source trees. The e2e setup in Task 2 must clone or fetch `anthropics/claude-plugins-official` at `6196a61bdeece7b9889ecda1e45bd7085788ae75` into a tmpdir and install from the selected local plugin source directories inside that checkout, because V1 install requires actual local plugin source trees.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `PINNED_SHA` exact value matches research.
    - Source assertion: `_targets.ts` documents four targets and soft-dep matrix flags exactly as specified.
    - Source assertion: `_targets.ts` or the e2e setup records how to locate each target's local source tree in the pinned upstream checkout.
    - Source assertion: fixture path includes the full SHA directory name.
  </acceptance_criteria>
  <done>Pinned upstream e2e inputs are deterministic and reviewable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement e2e coverage including required Pi-runtime smoke</name>
  <files>tests/e2e/resources-discover.test.ts, tests/e2e/install-soft-deps.test.ts, tests/e2e/pi-runtime-smoke.test.ts, tests/e2e/nightly-classification.test.ts</files>
  <read_first>tests/e2e/_pinned-sha.ts; tests/e2e/_targets.ts; tests/edge/register.test.ts; extensions/pi-claude-marketplace/index.ts; extensions/pi-claude-marketplace/shared/markers.ts; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 40-44 and 241-245; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 101-108 and 450-457; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 428-484 and 817-837</read_first>
  <behavior>
    - `resources-discover.test.ts` imports the extension entrypoint, registers it on a mock `ExtensionAPI`, stages fixture skills/prompts, invokes the registered `resources_discover` handler, and asserts returned paths include staged resources without restarting Pi.
    - `install-soft-deps.test.ts` runs the 2x2 soft-dep matrix for `code-simplifier` and `context7`, asserting unloaded soft deps produce the canonical warning and installed resources still surface.
    - `pi-runtime-smoke.test.ts` launches the real Pi runtime or package bin in a subprocess with isolated `HOME` and cwd, loads the extension through `index.ts`, dispatches a noninteractive command/smoke path, and asserts a non-error result. This is required Layer B coverage for D-01d.
    - `nightly-classification.test.ts` classifies failing floating-main runs with non-empty snapshot diff as upstream change and empty diff as regression.
  </behavior>
  <action>Create deterministic Layer A e2e tests using the mock Pi pattern from `tests/edge/register.test.ts`. Drive registered handlers directly through `index.ts`; use real disk tmp HOME/cwd and the pinned fixture marketplace. During e2e setup, clone or fetch `https://github.com/anthropics/claude-plugins-official.git` at `PINNED_SHA` into a tmpdir and install from the selected local plugin source directories; anonymous GitHub is the default and `GITHUB_TOKEN` may be used only for rate-limit fallback per D-01a. Do not invoke installed skill/command bodies or LLM turns. Because research blocked `@earendil-works/pi-agent-core`, implement required Layer B coverage in `pi-runtime-smoke.test.ts` with a subprocess Pi runtime/package-bin probe using isolated `HOME` and cwd. The smoke must fail closed: if the package bin cannot load the extension or no noninteractive dispatch surface is available, the test must fail with instructions to perform the blocking manual smoke gate before validation sign-off; do not skip or downgrade this test to Layer A-only. Implement nightly classification in test code or a helper imported by the nightly test so the workflow can distinguish upstream change from project regression.</action>
  <verify>
    <automated>PI_CM_E2E_REF=pinned node --test tests/e2e/resources-discover.test.ts tests/e2e/install-soft-deps.test.ts tests/e2e/pi-runtime-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `PI_CM_E2E_REF=pinned node --test tests/e2e/resources-discover.test.ts tests/e2e/install-soft-deps.test.ts tests/e2e/pi-runtime-smoke.test.ts` passes.
    - Source assertion: e2e tests import `extensions/pi-claude-marketplace/index.ts` directly.
    - Source assertion: e2e setup clones or fetches `anthropics/claude-plugins-official` at `6196a61bdeece7b9889ecda1e45bd7085788ae75` and uses real local source directories for V1 installs.
    - Source assertion: e2e tests do not call installed skill bodies, prompt bodies, or LLM APIs.
    - Behavior assertion: real Pi-runtime smoke is green, or validation sign-off is blocked by the failing smoke with manual gate instructions; Layer A alone is not accepted.
    - Behavior assertion: resources discovery assertion covers `/reload` by invoking the `resources_discover` event handler after staging resources.
  </acceptance_criteria>
  <done>E2E layer verifies NFR-2 and soft-dep behavior against pinned upstream source trees and includes required real Pi-runtime smoke.</done>
</task>

<task type="auto">
  <name>Task 3: Wire scripts and CI workflows per D-01a</name>
  <files>package.json, .github/workflows/ci.yml, .github/workflows/e2e-nightly.yml</files>
  <read_first>package.json; .github/workflows/ci.yml; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 26-31 and 247-254; .planning/phases/07-integration-pi-wiring/07-VALIDATION.md lines 16-33; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 699-769</read_first>
  <action>Update scripts so `npm test` runs unit/architecture/bridge/domain/edge/helper/orchestrator/persistence/presentation/shared/transaction tests and excludes `tests/e2e` and `tests/integration`; keep `npm run check` using unit-only `npm test`. Ensure `test:integration` is `node --test "tests/integration/**/*.test.ts"`, `test:e2e` is `PI_CM_E2E_REF=pinned node --test "tests/e2e/**/*.test.ts"`, and `test:e2e:nightly` is `PI_CM_E2E_REF=main node --test "tests/e2e/**/*.test.ts"`. Extend `.github/workflows/ci.yml` on Node 24 to run `npm run check`, `npm run test:integration`, `npm run test:e2e`, and `npm pack --dry-run`. Create `.github/workflows/e2e-nightly.yml` with `schedule` and `workflow_dispatch`, Node 24, `npm ci`, and `npm run test:e2e:nightly`.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/nightly-classification.test.ts && npm pack --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `package.json` contains exact scripts `test:e2e` and `test:e2e:nightly` with `PI_CM_E2E_REF=pinned` and `PI_CM_E2E_REF=main` respectively.
    - Source assertion: no script uses watch mode.
    - Source assertion: CI workflow includes `npm pack --dry-run`.
    - CLI assertion: targeted e2e classification and package dry-run pass locally; full phase gate remains required at plan-level verification.
  </acceptance_criteria>
  <done>PR and nightly validation commands are automated and separated from unit `npm test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub upstream -> e2e fixtures | Public upstream marketplace content enters tests. |
| E2E smoke -> local machine | Tests create temporary Pi-like roots and inspect resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering / Repudiation | upstream live tests | mitigate | Use pinned SHA fixtures in PR and floating-main nightly with snapshot-diff classification. |
| T-07-04 | Information Disclosure | smoke tests | mitigate | Use tmp HOME/cwd, do not invoke installed resource bodies or LLM turns, and avoid telemetry endpoints. |
| T-07-02 | Tampering | e2e install/update paths | mitigate | Run integration lock test in CI before e2e and assert no orphan resources in Plan 07-04. |
</threat_model>

<verification>
Task-local feedback uses targeted e2e commands above. Plan-level gate remains `npm run check && npm run test:integration && npm run test:e2e && npm pack --dry-run`; this gate must not pass if `tests/e2e/pi-runtime-smoke.test.ts` is failing or if the manual Pi-runtime smoke gate is still unresolved.
</verification>

<success_criteria>
PR e2e is deterministic at the pinned SHA, nightly e2e distinguishes upstream drift, and CI runs all Phase 7 gates.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-pi-wiring/07-05-SUMMARY.md`.
</output>
