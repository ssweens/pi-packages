---
phase: 07-integration-pi-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/platform/pi-api.ts
  - extensions/pi-claude-marketplace/presentation/soft-dep.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts
  - eslint.config.js
  - package.json
autonomous: true
requirements: [NFR-11]
must_haves:
  truths:
    - "Pi API imports have one auditable boundary for peer-dependency version changes."
    - "The project declares @mariozechner/pi-coding-agent >=0.73.1 as the peer floor."
  artifacts:
    - path: "extensions/pi-claude-marketplace/platform/pi-api.ts"
      provides: "Only direct @mariozechner/pi-coding-agent import surface plus soft-dep helpers"
    - path: "eslint.config.js"
      provides: "Static guard forbidding direct peer imports outside the wrapper"
    - path: "package.json"
      provides: "NFR-11 peer dependency floor"
  key_links:
    - from: "extension source files"
      to: "extensions/pi-claude-marketplace/platform/pi-api.ts"
      via: "type imports"
      pattern: "from \".*platform/pi-api.ts\""
---

<objective>
Create the Phase 7 Pi API boundary and peer-dependency floor.

Purpose: NFR-11 requires the Pi extension API to be declared as a peer dependency with a stable minimum version, and D-02 through D-05 require every non-test Pi API import to flow through one wrapper.
Output: `platform/pi-api.ts`, a `presentation/soft-dep.ts` compatibility shim, migrated imports, ESLint guard, and `package.json` peer floor.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-integration-pi-wiring/07-CONTEXT.md
@.planning/phases/07-integration-pi-wiring/07-RESEARCH.md
@.planning/phases/07-integration-pi-wiring/07-PATTERNS.md
@package.json
@eslint.config.js
@extensions/pi-claude-marketplace/presentation/soft-dep.ts
@extensions/pi-claude-marketplace/platform/git.ts
@extensions/pi-claude-marketplace/edge/register.ts

<interfaces>
Required wrapper exports per D-05 and research: `ExtensionAPI`, `ExtensionContext`, `ExtensionCommandContext`, `AutocompleteItem`, `ResourcesDiscoverEvent`, `ResourcesDiscoverResult`, `ToolDefinition`, `ToolInfo`, and `Tool` as an alias only if existing code expects it. `Tool` is not a top-level 0.73.1 export; use `ToolDefinition`/`ToolInfo` and alias deliberately if needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Pi API wrapper and soft-dep shim per D-02 through D-05</name>
  <files>extensions/pi-claude-marketplace/platform/pi-api.ts, extensions/pi-claude-marketplace/presentation/soft-dep.ts</files>
  <read_first>extensions/pi-claude-marketplace/presentation/soft-dep.ts; extensions/pi-claude-marketplace/platform/git.ts; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 46-52; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 209-228; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 106-188</read_first>
  <behavior>
    - `hasLoadedPiSubagents(pi)` returns true only when `pi.getAllTools()` contains a tool whose `name` is exactly `subagent`.
    - `hasLoadedPiMcpAdapter(pi)` returns true when a tool name is `mcp` or `tool.sourceInfo.source` contains `pi-mcp-adapter`.
    - Soft-dep helper exceptions from `pi.getAllTools()` return false, preserving existing degraded behavior.
  </behavior>
  <action>Create `extensions/pi-claude-marketplace/platform/pi-api.ts` as the only direct import/re-export surface from `@mariozechner/pi-coding-agent` per D-02 and D-05. Move `softDepStatus`, `hasLoadedPiSubagents`, `hasLoadedPiMcpAdapter`, `subagentWarningIfNeeded`, and `mcpAdapterWarningIfNeeded` from `presentation/soft-dep.ts` into the wrapper per D-03. Replace `presentation/soft-dep.ts` with a one-line re-export shim that exports those five names from `../platform/pi-api.ts`. Do not create an adapter class; the locked design is a thin typed re-export plus helpers.</action>
  <verify>
    <automated>npm test -- tests/presentation/soft-dep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `extensions/pi-claude-marketplace/platform/pi-api.ts` contains the only production `from "@mariozechner/pi-coding-agent"` import after Task 2.
    - Source assertion: `extensions/pi-claude-marketplace/presentation/soft-dep.ts` contains no `getAllTools` implementation and re-exports from `../platform/pi-api.ts`.
    - Behavior assertion: existing soft-dep tests still pass without changing warning text constants.
  </acceptance_criteria>
  <done>The platform wrapper owns Pi API types and soft-dep helpers, while old presentation imports remain source-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Pi API imports and enforce wrapper boundary per D-04</name>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/tools.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts, eslint.config.js</files>
  <read_first>extensions/pi-claude-marketplace/edge/register.ts; extensions/pi-claude-marketplace/edge/handlers/tools.ts; extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts; extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts; extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts; eslint.config.js; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 647-696</read_first>
  <action>Migrate every production import of `@mariozechner/pi-coding-agent` under `extensions/pi-claude-marketplace/` to the correct relative `platform/pi-api.ts` import per D-04. Add an ESLint `no-restricted-imports` rule for extension files that forbids `@mariozechner/pi-coding-agent` except in `extensions/pi-claude-marketplace/platform/pi-api.ts`; keep tests exempt via the existing `tests/**/*.ts` override. Preserve type-only imports where they are type-only; do not introduce runtime imports from the peer outside the wrapper.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `npm run lint` passes.
    - Source assertion: a non-comment grep for `@mariozechner/pi-coding-agent` under `extensions/pi-claude-marketplace/**/*.ts` returns only `extensions/pi-claude-marketplace/platform/pi-api.ts`.
    - Source assertion: tests may still import peer types directly; extension production files may not.
  </acceptance_criteria>
  <done>ESLint blocks peer-import drift and all production callsites import through `platform/pi-api.ts`.</done>
</task>

<task type="auto">
  <name>Task 3: Pin peer-dep floor at 0.73.1 per D-16</name>
  <files>package.json</files>
  <read_first>package.json; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 49-51 and 123-132; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 77-80</read_first>
  <action>Update `package.json` `peerDependencies["@mariozechner/pi-coding-agent"]` from `>=0.70.6` to `>=0.73.1` per D-16, using the completed research verification that 0.73.1 preserves the required wrapper surface. Do not add `@earendil-works/pi-agent-core` as a dependency in this plan because research found its published declarations do not support the locked smoke-driver surface.</action>
  <verify>
    <automated>npm run typecheck && npm pack --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `package.json` peer dependency value is exactly `>=0.73.1`.
    - CLI assertion: `npm pack --dry-run` completes and lists the extension package without manifest errors.
  </acceptance_criteria>
  <done>NFR-11 peer floor is declared and package manifest validates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi runtime -> extension wrapper | External Pi API types and tools enter project code through `platform/pi-api.ts`. |
| CI/package manifest -> consumers | Peer dependency declaration determines accepted runtime API floor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Pi API imports | mitigate | Enforce `@mariozechner/pi-coding-agent` imports only in `platform/pi-api.ts` with ESLint `no-restricted-imports`. |
| T-07-03 | Tampering / Repudiation | peer-dep floor | mitigate | Pin `@mariozechner/pi-coding-agent` peer floor to `>=0.73.1` and validate with `npm run typecheck && npm pack --dry-run`. |
</threat_model>

<verification>
Run `npm run lint`, `npm run typecheck`, and `npm pack --dry-run` after the plan.
</verification>

<success_criteria>
`platform/pi-api.ts` is the only production peer import surface, ESLint enforces that boundary, and package metadata declares `@mariozechner/pi-coding-agent >=0.73.1`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-pi-wiring/07-01-SUMMARY.md`.
</output>
