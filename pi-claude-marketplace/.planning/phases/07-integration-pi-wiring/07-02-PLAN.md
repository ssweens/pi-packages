---
phase: 07-integration-pi-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/manifest.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/edge/completions/data.ts
  - tests/architecture/manifest-read-seam.test.ts
autonomous: true
requirements: [NFR-8]
must_haves:
  truths:
    - "Marketplace manifests are read through one domain seam."
    - "An architecture test fails if any non-domain module reads marketplace.json directly."
  artifacts:
    - path: "extensions/pi-claude-marketplace/domain/manifest.ts"
      provides: "loadMarketplaceManifest(manifestPath) chokepoint"
    - path: "tests/architecture/manifest-read-seam.test.ts"
      provides: "NFR-8 seam enforcement"
  key_links:
    - from: "marketplace/plugin/edge modules"
      to: "domain/manifest.ts"
      via: "loadMarketplaceManifest import"
      pattern: "loadMarketplaceManifest"
---

<objective>
Establish and verify the NFR-8 manifest-read seam.

Purpose: Phase 7 must make manifest mtime caching addable through one chokepoint without adding the cache layer that CONTEXT explicitly defers.
Output: `loadMarketplaceManifest` plus migrated direct readers and an architecture test.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-integration-pi-wiring/07-CONTEXT.md
@.planning/phases/07-integration-pi-wiring/07-RESEARCH.md
@.planning/phases/07-integration-pi-wiring/07-PATTERNS.md
@extensions/pi-claude-marketplace/domain/manifest.ts
@tests/architecture/no-orchestrator-network.test.ts

<interfaces>
Create `loadMarketplaceManifest(manifestPath: string): Promise<MarketplaceManifest>` in `domain/manifest.ts`. It must parse the JSON file and validate with the existing `MARKETPLACE_VALIDATOR` / schema convention in that file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add loadMarketplaceManifest domain seam per D-14</name>
  <files>extensions/pi-claude-marketplace/domain/manifest.ts</files>
  <read_first>extensions/pi-claude-marketplace/domain/manifest.ts; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 315-320 and 398-402; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 354-381</read_first>
  <behavior>
    - Valid `marketplace.json` content resolves to `MarketplaceManifest`.
    - Invalid JSON or schema-invalid content throws through the same validator error style as existing manifest parsing.
  </behavior>
  <action>Add `loadMarketplaceManifest(manifestPath: string): Promise<MarketplaceManifest>` to `extensions/pi-claude-marketplace/domain/manifest.ts`. Use `node:fs/promises` `readFile`, `JSON.parse`, and the existing marketplace validator convention. Keep the function focused on manifest-path reads only; do not implement mtime caching, cache invalidation, or a cache object because D-14 requires the seam only and CONTEXT deferred ideas exclude cache implementation.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `domain/manifest.ts` exports `loadMarketplaceManifest`.
    - Source assertion: the only production `readFile` call whose argument is a marketplace manifest path is inside `domain/manifest.ts` after Task 2.
  </acceptance_criteria>
  <done>Domain manifest file owns the single read chokepoint.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all direct marketplace.json readers to the seam</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/edge/completions/data.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts; extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts; extensions/pi-claude-marketplace/orchestrators/plugin/install.ts; extensions/pi-claude-marketplace/orchestrators/plugin/list.ts; extensions/pi-claude-marketplace/orchestrators/plugin/update.ts; extensions/pi-claude-marketplace/edge/completions/data.ts; extensions/pi-claude-marketplace/domain/manifest.ts; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 315-320</read_first>
  <action>Replace direct `readFile` + `JSON.parse` + marketplace-validator logic for `marketplace.json` paths with `loadMarketplaceManifest(manifestPath)`. Preserve each caller's existing error wrapping, soft-fail behavior, and message text: marketplace add/update failures remain their existing user-facing errors, plugin list manifest failures still render `[warning] could not load manifest: <reason>`, and completion manifest failures still soft-fail per TC-8. Do not alter network behavior.</action>
  <verify>
    <automated>npm test -- tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/update.test.ts tests/edge/completions/data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: existing marketplace/plugin/completion tests pass.
    - Source assertion: callers import `loadMarketplaceManifest` from `../../domain/manifest.ts` or the correct relative path.
    - Source assertion: no migrated caller retains a `readFile` call with `marketplace.json` in the same read context.
  </acceptance_criteria>
  <done>All production manifest-path reads route through the domain seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add manifest read seam architecture test</name>
  <files>tests/architecture/manifest-read-seam.test.ts</files>
  <read_first>tests/architecture/no-orchestrator-network.test.ts; extensions/pi-claude-marketplace/domain/manifest.ts; .planning/phases/07-integration-pi-wiring/07-VALIDATION.md lines 41-44 and 52-56; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 574-621</read_first>
  <behavior>
    - The test fails when any file under `extensions/pi-claude-marketplace/` except `domain/manifest.ts` contains a `readFile` or `fs.readFile` call context that references `marketplace.json`.
    - The test strips comments before matching so header prose does not self-invalidate the gate.
  </behavior>
  <action>Create `tests/architecture/manifest-read-seam.test.ts` using the existing architecture-test style. Scan `extensions/pi-claude-marketplace/**/*.ts`, exclude `extensions/pi-claude-marketplace/domain/manifest.ts`, strip block comments and line comments, and report explicit offender file paths when a `readFile` / `fs.readFile` call context contains `marketplace.json`. Use source scanning or TypeScript AST; keep the failure message actionable.</action>
  <verify>
    <automated>node --test tests/architecture/manifest-read-seam.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `node --test tests/architecture/manifest-read-seam.test.ts` passes.
    - Source assertion: test name references NFR-8 or manifest read seam.
    - Source assertion: the test excludes comments before applying its forbidden pattern.
  </acceptance_criteria>
  <done>NFR-8 seam is protected by an automated architecture gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream/local manifest file -> parser | Untrusted marketplace JSON enters domain validation. |
| Orchestrators -> domain manifest seam | Business flows depend on one read chokepoint preserving validation and error behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | manifest-path reads | mitigate | Route every `marketplace.json` read through `loadMarketplaceManifest` and enforce with `manifest-read-seam.test.ts`. |
| T-07-03 | Tampering / Repudiation | upstream manifest drift | transfer | E2E pinning/classification is handled in Plan 07-05; this plan only centralizes reads. |
</threat_model>

<verification>
Run `node --test tests/architecture/manifest-read-seam.test.ts` and the migrated caller test files.
</verification>

<success_criteria>
The architecture test proves a single manifest read seam and migrated callers keep their existing behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-pi-wiring/07-02-SUMMARY.md`.
</output>
