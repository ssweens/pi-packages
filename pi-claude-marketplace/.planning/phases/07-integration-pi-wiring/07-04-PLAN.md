---
phase: 07-integration-pi-wiring
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-03]
files_modified:
  - package.json
  - package-lock.json
  - extensions/pi-claude-marketplace/persistence/locations.ts
  - extensions/pi-claude-marketplace/transaction/with-state-guard.ts
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/shared/markers.ts
  - tests/transaction/with-state-guard.test.ts
  - tests/architecture/markers-snapshot.test.ts
  - tests/integration/concurrent-install.test.ts
  - tests/integration/concurrent-install-child.ts
autonomous: true
requirements: [NFR-3]
must_haves:
  truths:
    - "Two Pi processes mutating the same scope cannot last-writer-wins state.json into state/disk drift."
    - "The loser fails cleanly with the Phase 7 lock marker and leaves no orphan resources."
  artifacts:
    - path: "extensions/pi-claude-marketplace/transaction/with-state-guard.ts"
      provides: "Cross-process per-scope lock around load-mutate-save"
    - path: "tests/integration/concurrent-install.test.ts"
      provides: "Multi-process race verification"
  key_links:
    - from: "with-state-guard.ts"
      to: "persistence/locations.ts"
      via: "stateLockFile"
      pattern: "stateLockFile"
---

<objective>
Add cross-process state locking and verify retry-safe failure behavior.

Purpose: NFR-3 requires operations to be safe to retry; Phase 7 D-06 through D-09 close the cross-process race that could create state/disk drift.
Output: `proper-lockfile` dependency, `STATE_LOCK_HELD_PREFIX`, `StateLockHeldError`, `stateLockFile`, locked `withStateGuard`, and concurrency tests.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-integration-pi-wiring/07-CONTEXT.md
@.planning/phases/07-integration-pi-wiring/07-RESEARCH.md
@.planning/phases/07-integration-pi-wiring/07-PATTERNS.md
@package.json
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
@extensions/pi-claude-marketplace/shared/markers.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@tests/transaction/with-state-guard.test.ts

<interfaces>
Add `stateLockFile(locations: ScopedLocations): string` or an equivalent readonly `stateLockFile` field that resolves to `<scopeRoot>/pi-claude-marketplace/.state-lock`. Add `StateLockHeldError` carrying scope/path details. Add `STATE_LOCK_HELD_PREFIX = "Another pi-claude-marketplace operation is in progress for"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add state lock primitive and marker per D-06 through D-09</name>
  <files>package.json, package-lock.json, extensions/pi-claude-marketplace/persistence/locations.ts, extensions/pi-claude-marketplace/shared/errors.ts, extensions/pi-claude-marketplace/shared/markers.ts, tests/architecture/markers-snapshot.test.ts</files>
  <read_first>package.json; package-lock.json; extensions/pi-claude-marketplace/persistence/locations.ts; extensions/pi-claude-marketplace/shared/errors.ts; extensions/pi-claude-marketplace/shared/markers.ts; tests/architecture/markers-snapshot.test.ts; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 60-68; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 276-351 and 624-642</read_first>
  <behavior>
    - Marker snapshot asserts `STATE_LOCK_HELD_PREFIX` exactly equals `Another pi-claude-marketplace operation is in progress for`.
    - Lockfile path resolves under `locations.extensionRoot` as `.state-lock`.
  </behavior>
  <action>Add runtime dependency `proper-lockfile@^4.1.2` and dev dependency `@types/proper-lockfile@^4.1.4`. Add `STATE_LOCK_HELD_PREFIX` to `shared/markers.ts` with a Phase 7 extension comment and a separate marker snapshot test. Add `StateLockHeldError` to `shared/errors.ts` carrying at least the lock path or scope root. Add a `stateLockFile` helper or field in `persistence/locations.ts` for `<scopeRoot>/pi-claude-marketplace/.state-lock`, using existing containment conventions where the file supports them.</action>
  <verify>
    <automated>npm install && node --test tests/architecture/markers-snapshot.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `package.json` contains `proper-lockfile` in dependencies and `@types/proper-lockfile` in devDependencies.
    - Source assertion: `STATE_LOCK_HELD_PREFIX` exact string is present once in `shared/markers.ts` and asserted in `markers-snapshot.test.ts`.
    - Source assertion: lock path helper yields `.state-lock` below `extensionRoot`.
  </acceptance_criteria>
  <done>State lock dependencies, marker, error, and path primitive exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap withStateGuard load-mutate-save in proper-lockfile</name>
  <files>extensions/pi-claude-marketplace/transaction/with-state-guard.ts, tests/transaction/with-state-guard.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/transaction/with-state-guard.ts; tests/transaction/with-state-guard.test.ts; extensions/pi-claude-marketplace/persistence/state-io.ts; extensions/pi-claude-marketplace/persistence/locations.ts; extensions/pi-claude-marketplace/shared/errors.ts; .planning/phases/07-integration-pi-wiring/07-RESEARCH.md lines 230-254 and 292-300; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 255-273 and 533-571</read_first>
  <behavior>
    - `withStateGuard` acquires the per-scope lock before `loadState` and releases after `saveState`.
    - `retries: 0`, `stale: 10000`, `update: 2000`, `realpath: false`, and `lockfilePath: stateLockFile(locations)` are used.
    - Lock-held errors throw `StateLockHeldError` immediately.
    - Mutate throws and save throws both release the lock via `finally`.
  </behavior>
  <action>Update `withStateGuard` to call `proper-lockfile.lock(locations.extensionRoot, { lockfilePath: stateLockFile(locations), realpath: false, retries: 0, stale: 10000, update: 2000 })` before `loadState`. Release the returned function in `finally` after the mutate/save critical section. Wrap lock-acquisition failure in `StateLockHeldError`; preserve the public function signature and existing save-only-on-no-throw contract. Update the docstring to say cross-process safety is now provided by the Phase 7 per-scope lock, not only by write-file-atomic byte safety.</action>
  <verify>
    <automated>node --test tests/transaction/with-state-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: transaction tests pass.
    - Behavior assertion: a pre-held lock causes immediate `StateLockHeldError` without running mutate.
    - Behavior assertion: a second call after mutate/save failure can acquire the lock, proving release occurred.
  </acceptance_criteria>
  <done>`withStateGuard` protects the full load-mutate-save critical section across processes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add multi-process concurrent install integration test per D-15</name>
  <files>tests/integration/concurrent-install.test.ts, tests/integration/concurrent-install-child.ts</files>
  <read_first>tests/edge/handlers/plugin/install.test.ts; tests/transaction/with-state-guard.test.ts; extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts; extensions/pi-claude-marketplace/orchestrators/plugin/install.ts; extensions/pi-claude-marketplace/shared/markers.ts; .planning/phases/07-integration-pi-wiring/07-VALIDATION.md lines 41-44 and 52-56; .planning/phases/07-integration-pi-wiring/07-CONTEXT.md lines 73-76; .planning/phases/07-integration-pi-wiring/07-PATTERNS.md lines 487-530</read_first>
  <behavior>
    - Same-plugin race: one child succeeds, one child fails with `STATE_LOCK_HELD_PREFIX` or a documented bridge-level same-target collision; state records only one install.
    - Different-plugin same-scope race: one child succeeds, one child fails with `STATE_LOCK_HELD_PREFIX`; `state.json` contains exactly one plugin record.
    - Disk resources after each race match state records; no orphan skills/prompts/agents/MCP entries remain for the losing plugin.
  </behavior>
  <action>Create `tests/integration/concurrent-install.test.ts` using `node:child_process.fork` and an IPC child module `tests/integration/concurrent-install-child.ts`. Use isolated tmp `HOME`, cwd, and a shared scope root for each test case. The child should run the real install command path and report `{ ok, message }` via IPC, not stdout. Include the two D-15 cases: same-plugin and different-plugin same-marketplace. Assert `STATE_LOCK_HELD_PREFIX` for lock losers and exact state/disk post-conditions. Do not add watch-mode or long-running sleeps; use deterministic process synchronization where possible.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/concurrent-install.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `npm run test:integration -- tests/integration/concurrent-install.test.ts` passes once Plan 07-05 defines the script; before that, `node --test tests/integration/concurrent-install.test.ts` passes.
    - Behavior assertion: loser error message includes `Another pi-claude-marketplace operation is in progress for` in the normal lock-held case.
    - Source assertion: child process reports via IPC and does not rely on stdout parsing.
  </acceptance_criteria>
  <done>Cross-process race behavior is covered by an automated integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process A -> state.json <- Process B | Two Pi processes mutate the same scope concurrently. |
| Lockfile -> transaction guard | File lock status controls entry to state mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | state.json and staged resources | mitigate | Acquire `proper-lockfile` per-scope lock before `loadState`, release after `saveState`, and fail fast with `StateLockHeldError`. |
| T-07-01 | Tampering | state lock path | mitigate | Derive `.state-lock` from `ScopedLocations.extensionRoot`; no user-supplied lock path. |
</threat_model>

<verification>
Run `node --test tests/transaction/with-state-guard.test.ts tests/architecture/markers-snapshot.test.ts` and the concurrent integration test.
</verification>

<success_criteria>
Concurrent same-scope installs cannot overwrite state; one process wins cleanly and the loser leaves state/disk retry-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-pi-wiring/07-04-SUMMARY.md`.
</output>
