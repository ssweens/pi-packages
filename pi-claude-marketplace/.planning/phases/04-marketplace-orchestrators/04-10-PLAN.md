---
phase: 04-marketplace-orchestrators
plan: 10
type: execute
wave: 3
depends_on: ["04-02", "04-05", "04-06", "04-07", "04-08", "04-09"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts
autonomous: true
requirements:
  - MU-2
  - MU-3
tags: [phase-04, documentation, supersession, d-14]
must_haves:
  truths:
    - "REQUIREMENTS.md MU-2 row marked with strikethrough + supersession note pointing at Phase 4 D-14 (mirrors the MA-7 / D-21 row format)"
    - "REQUIREMENTS.md MU-3 row marked with strikethrough + supersession note"
    - "REQUIREMENTS.md Traceability table updates MU-2 and MU-3 from 'Phase 4 | Pending' to '-- | Superseded by Phase 4 D-14'"
    - "REQUIREMENTS.md Per-phase counts row for Phase 4 updated to note MU-2/MU-3 supersession (parallel to MA-7 entry)"
    - "PROJECT.md Key Decisions table gains a new row for D-23 (Phase 4 D-14): 'Adopt follow-upstream-blindly semantics for marketplace update; supersede MU-2/MU-3' with the same '-- Locked' outcome marker as D-21"
    - "PRD §5.1.4 is NOT modified (PRD is the historical baseline; supersession lives in .planning/ artifacts only)"
    - "orchestrators/marketplace/index.ts barrel re-exports addMarketplace, removeMarketplace, listMarketplaces, updateMarketplace, updateAllMarketplaces, setMarketplaceAutoupdate (plus their option types) -- finalizing the public surface after Wave 2 ships the orchestrators"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "MU-2/MU-3 supersession note + Traceability + Per-phase counts updates"
      contains: "superseded by Phase 4 D-14"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row for the D-14 follow-upstream-blindly contract change"
      contains: "follow-upstream-blindly"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts"
      provides: "Final barrel re-export for all five Phase 4 orchestrator entry points + their option types"
      contains: "addMarketplace"
  key_links:
    - from: ".planning/REQUIREMENTS.md (MU-2 / MU-3 list items)"
      to: "Phase 4 D-14 in .planning/phases/04-marketplace-orchestrators/04-CONTEXT.md"
      via: "supersession note text"
      pattern: "superseded by Phase 4 D-14"
    - from: ".planning/PROJECT.md (Key Decisions table)"
      to: "Phase 4 D-14 supersession"
      via: "new table row mirroring D-21 format"
      pattern: "D-23\\|D-22.*follow-upstream-blindly"
---

<objective>
Record the D-14 user-contract change in the two `.planning/` documents that downstream phases consult. Mirrors the MA-7 / D-21 supersession that Plan 01-04 already shipped.

D-14 (in CONTEXT.md): "The local marketplace clone is read-only by contract -- the extension only clones, fetches, and checks out; it never commits, pushes, or modifies the working tree. There is no local work to 'clobber' and no scenario where local-vs-upstream divergence is a real user concern. `marketplace update` therefore follows upstream blindly." This supersedes:

- **MU-2** (PRD §5.1.4): "GitHub sources `git fetch` then `git pull --ff-only` (symbolic HEAD) or re-checkout stored ref (detached HEAD)" -- D-14 replaces with `fetch + forceUpdateRef + checkout` (no `pull`).
- **MU-3** (PRD §5.1.4): "Non-fast-forward divergence surfaces as error; recovery is `marketplace remove` + re-add" -- D-14 makes divergence non-applicable (the local clone is never altered, so divergence cannot exist).

Two file edits:

1. **`.planning/REQUIREMENTS.md`**: strike through MU-2 and MU-3 in the v1 Requirements list (parallel to the MA-7 line); update the Traceability table for both REQ-IDs (status changes from `Pending` to `Superseded by Phase 4 D-14`); update the Per-phase counts footnote for Phase 4 (parallel to the existing MA-7 supersession note).
2. **`.planning/PROJECT.md`**: append a new row to the Key Decisions table (after the D-22 row) recording the D-14 supersession.

The PRD (`docs/prd/pi-claude-marketplace-prd.md`) §5.1.4 is NOT modified -- the PRD is the historical baseline; the supersession lives in `.planning/` artifacts only, parallel to how the D-21 / MA-7 supersession was handled.

Output: two markdown files updated with strikethrough + supersession notes; one new Key Decisions row.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-marketplace-orchestrators/04-CONTEXT.md
@.planning/phases/04-marketplace-orchestrators/04-RESEARCH.md
@.planning/phases/01-foundations-toolchain/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md -- strikethrough MU-2 and MU-3 + Traceability + Per-phase counts</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 17 -- existing MA-7 strikethrough format; the exact pattern to mirror for MU-2/MU-3; lines 44-45 -- the current MU-2/MU-3 list items; line 357 -- the existing MA-7 traceability row; lines 374-376 -- the MU-2 + MU-3 traceability rows; lines 555-565 -- the coverage notes + Per-phase counts table)
    - .planning/phases/04-marketplace-orchestrators/04-CONTEXT.md (D-14 section + D-14 supersession effect description -- the canonical text Plan 04-10 references in the supersession notes)
    - .planning/phases/01-foundations-toolchain/01-04-SUMMARY.md (Plan 01-04 shipped the MA-7 / D-21 supersession; precedent for the EXACT format to mirror -- read this first to see what Plan 01-04's REQUIREMENTS.md edit looked like and duplicate the structure)
    - docs/prd/pi-claude-marketplace-prd.md §5.1.4 (the PRD source for MU-2/MU-3; NOT modified by this task -- referenced only to confirm the original spec text)
  </read_first>
  <action>
    Make four edits to `.planning/REQUIREMENTS.md`. Use Edit (not Write -- the file is large and we only modify specific sections).

    **Edit (A): MU-2 list item.** Find the line currently reading:

    ```
    - [ ] **MU-2**: GitHub sources `git fetch` then `git pull --ff-only` (symbolic HEAD) or re-checkout stored ref (detached HEAD)
    ```

    Replace with:

    ```
    - [x] ~~**MU-2**: GitHub sources `git fetch` then `git pull --ff-only` (symbolic HEAD) or re-checkout stored ref (detached HEAD)~~ (**superseded by Phase 4 D-14**: the local marketplace clone is read-only by contract; `marketplace update` follows upstream blindly via `fetch + forceUpdateRef + checkout` -- see `.planning/phases/04-marketplace-orchestrators/04-CONTEXT.md` D-14. This is a deliberate user-contract change recorded in PROJECT.md Key Decisions.)
    ```

    **Edit (B): MU-3 list item.** Find the line currently reading:

    ```
    - [ ] **MU-3**: Non-fast-forward divergence surfaces as error; recovery is `marketplace remove` + re-add
    ```

    Replace with:

    ```
    - [x] ~~**MU-3**: Non-fast-forward divergence surfaces as error; recovery is `marketplace remove` + re-add~~ (**superseded by Phase 4 D-14**: the local clone is never altered, so non-fast-forward divergence cannot exist as a user-visible failure mode; `marketplace update` overwrites the local ref unconditionally -- see `.planning/phases/04-marketplace-orchestrators/04-CONTEXT.md` D-14.)
    ```

    **Edit (C): Traceability table rows for MU-2 and MU-3.** Find the lines:

    ```
    | MU-2        | Phase 4 | Pending |
    | MU-3        | Phase 4 | Pending |
    ```

    Replace with (mirrors the MA-7 traceability row format):

    ```
    | MU-2        | --      | Superseded by Phase 4 D-14 |
    | MU-3        | --      | Superseded by Phase 4 D-14 |
    ```

    **Edit (D): Coverage block + Per-phase counts.** Find the block starting around line 552 that currently reads:

    ```
    **Coverage:**

    - v1 requirements: 200 total (file footer previously claimed 134; corrected here)
    - Mapped to phases: 199 (99.5%) -- MA-7 superseded by D-21 (Phase 1 adopted isomorphic-git, removing the "git CLI not found" failure mode)
    - Unmapped: 0 (MA-7 is superseded, not unmapped)
    ```

    Replace with:

    ```
    **Coverage:**

    - v1 requirements: 200 total (file footer previously claimed 134; corrected here)
    - Mapped to phases: 197 (98.5%) -- MA-7 superseded by D-21 (Phase 1 adopted isomorphic-git, removing the "git CLI not found" failure mode); MU-2 and MU-3 superseded by Phase 4 D-14 (follow-upstream-blindly semantics; the local marketplace clone is read-only by contract, so non-fast-forward divergence cannot occur)
    - Unmapped: 0 (MA-7, MU-2, MU-3 are superseded, not unmapped)
    ```

    Also update the Phase 4 row in the Per-phase counts table (around line 565) -- the current row reads:

    ```
    | Phase 4: Marketplace Orchestrators            | 43 (MA-1..6, MA-8..11 (MA-7 superseded by D-21), MR-1..8, ML-1..4, MU-1..9, MAU-1..4, SC-5..6, RH-1..5, NFR-5)        |
    ```

    Replace with:

    ```
    | Phase 4: Marketplace Orchestrators            | 41 (MA-1..6, MA-8..11 (MA-7 superseded by D-21), MR-1..8, ML-1..4, MU-1, MU-4..9 (MU-2, MU-3 superseded by Phase 4 D-14), MAU-1..4, SC-5..6, RH-1..5, NFR-5) |
    ```

    Notes:
    - The 43 → 41 count adjustment reflects MU-2 and MU-3 being removed from the active Phase 4 owned set (43 - 2 = 41).
    - The supersession note text MUST match the language in CONTEXT.md D-14 verbatim where it says "follow-upstream-blindly semantics" and "the local marketplace clone is read-only by contract" -- these are user-facing contract-change phrases.
    - Do NOT modify the PRD (`docs/prd/pi-claude-marketplace-prd.md`). The PRD is the historical baseline; supersession lives only in `.planning/` artifacts (parallel to how D-21 / MA-7 was handled).
  </action>
  <verify>
    <automated>grep -q "~~\*\*MU-2\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "~~\*\*MU-3\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "superseded by Phase 4 D-14" .planning/REQUIREMENTS.md &amp;&amp; grep -q "| MU-2.*Superseded by Phase 4 D-14" .planning/REQUIREMENTS.md &amp;&amp; grep -q "| MU-3.*Superseded by Phase 4 D-14" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Mapped to phases: 197" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Phase 4: Marketplace Orchestrators.*41 " .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - MU-2 and MU-3 list items both have `~~**MU-N**: ...~~` strikethrough + a parenthetical supersession note ending with the CONTEXT.md anchor
    - The Traceability table shows `Superseded by Phase 4 D-14` for both MU-2 and MU-3 (Phase column = `--` to match the MA-7 precedent)
    - Coverage block reads `Mapped to phases: 197 (98.5%)` and lists all three superseded REQ-IDs
    - Per-phase counts row for Phase 4 reads `41 (...MU-1, MU-4..9 (MU-2, MU-3 superseded by Phase 4 D-14)...)`
    - PRD `docs/prd/pi-claude-marketplace-prd.md` is NOT modified by this task
  </done>
</task>

<task type="auto">
  <name>Task 2: Add D-23 (Phase 4 D-14 supersession) row to PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 96-110 -- Key Decisions section + the D-21 and D-22 rows; the format and outcome marker '-- Locked' to mirror exactly)
    - .planning/phases/04-marketplace-orchestrators/04-CONTEXT.md (D-14 section -- the canonical text used in the Rationale column)
    - .planning/phases/01-foundations-toolchain/01-04-SUMMARY.md (Plan 01-04 shipped the D-21 row; precedent for the format)
  </read_first>
  <action>
    Add a new row to the Key Decisions table in `.planning/PROJECT.md` immediately after the existing D-22 row (which is the last row in the table at line 110). The new row mirrors the D-21 row's format exactly: bold decision name with date prefix, plain-text rationale, `-- Locked` outcome.

    Find the line currently reading:

    ```
    | **D-22 (2026-05-09):** Zero `pi.registerTool` calls in Phase 1; LLM tool surface deferred to Phase 6 (`edge/handlers/list.ts`) | Phase 1 ships only the `/claude:plugin` slash command + `resources_discover` event handler. The LLM tool surface (`claude_plugin_list`/`install`/`uninstall`/etc.) is a Phase 6 deliverable. Regression-guarded by `tests/shared/index-smoke.test.ts` (asserts `tools.length === 0`). Resolved at the Plan 01-07 checkpoint (`approved-zero-tools`). | -- Locked  |
    ```

    Insert IMMEDIATELY AFTER it (on a new line, before the next section header `## Evolution`):

    ```
    | **D-23 (2026-05-10):** Adopt follow-upstream-blindly semantics for `marketplace update`; supersede PRD MU-2 and MU-3                | The local marketplace clone is read-only by contract -- the extension only clones, fetches, and checks out; it never commits, pushes, or modifies the working tree. Local-vs-upstream divergence cannot occur, so `pull --ff-only` and "non-fast-forward divergence as error" are no longer applicable. `marketplace update` therefore overrides the local branch ref to the remote SHA via `gitOps.forceUpdateRef` + `gitOps.checkout` (or checks out a detached SHA directly). Phase 4 implements this in `orchestrators/marketplace/update.ts` per CONTEXT.md D-14. Recorded by Plan 04-10. | -- Locked  |
    ```

    Notes:
    - The new row is decision **D-23**, dated 2026-05-10 (the date Phase 4 was planned). D-22 was 2026-05-09; D-23 is the next sequential decision number.
    - The "outcome" column uses `-- Locked` (two dashes + space + "Locked"), exactly matching D-21 and D-22.
    - The double-space spacing inside the cell (e.g., `| Phase 4...` after the Rationale column) follows the existing table's column-alignment convention. Use Edit with the verbatim content above; mdformat should not reflow inside cells.
    - Do NOT modify any earlier rows in the table.
    - Do NOT modify any other section of PROJECT.md.
  </action>
  <verify>
    <automated>grep -q "D-23 (2026-05-10).*follow-upstream-blindly" .planning/PROJECT.md &amp;&amp; grep -q "supersede PRD MU-2 and MU-3" .planning/PROJECT.md &amp;&amp; grep -q "Recorded by Plan 04-10" .planning/PROJECT.md &amp;&amp; ! grep -q "D-23.*Pending" .planning/PROJECT.md</automated>
  </verify>
  <done>
    - PROJECT.md Key Decisions table has a new row for D-23 dated 2026-05-10
    - The row's Decision cell mentions `follow-upstream-blindly`
    - The row's Rationale cell mentions `supersede PRD MU-2 and MU-3` and ends with `Recorded by Plan 04-10.`
    - The row's Outcome cell is `-- Locked` (matching D-21 and D-22 format)
    - No other PROJECT.md content modified
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize orchestrators/marketplace/index.ts barrel re-export</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts (existing barrel from Plan 04-02 Task 2 -- currently re-exports DEFAULT_GIT_OPS, applyAutoupdateFlip, cascadeUnstagePlugin, formatErrorWithCauses, resolveScopeFromState plus types AutoupdateFlipResult, GitOps, UnstageOutcome)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (Plan 04-05 -- exports addMarketplace + AddMarketplaceOptions)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (Plan 04-06 -- exports removeMarketplace + RemoveMarketplaceOptions)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts (Plan 04-07 -- exports listMarketplaces + ListMarketplacesOptions)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts (Plan 04-08 -- exports updateMarketplace, updateAllMarketplaces + UpdateMarketplaceOptions, UpdateAllMarketplacesOptions)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts (Plan 04-09 -- exports setMarketplaceAutoupdate + AutoupdateOptions)
    - extensions/pi-claude-marketplace/bridges/skills/index.ts (canonical barrel-index pattern: one re-export line per public symbol; type re-exports grouped at bottom)
  </read_first>
  <action>
    Replace the current `orchestrators/marketplace/index.ts` content with the full barrel below. The file consolidates the cross-subcommand helpers from Plan 04-02 with the per-subcommand entry points from Plans 04-05..04-09. Verbatim:

    ```typescript
    // orchestrators/marketplace/index.ts
    //
    // Barrel re-export for the marketplace orchestrators layer (Phase 4).
    // Exposes the cross-subcommand helpers from `shared.ts` plus the five
    // per-subcommand entry points that Phase 6's edge layer wires into
    // /claude:plugin marketplace <subcommand>.

    // Cross-subcommand helpers (from shared.ts).
    export {
      DEFAULT_GIT_OPS,
      applyAutoupdateFlip,
      cascadeUnstagePlugin,
      formatErrorWithCauses,
      resolveScopeFromState,
    } from "./shared.ts";

    export type {
      AutoupdateFlipResult,
      GitOps,
      UnstageOutcome,
    } from "./shared.ts";

    // Per-subcommand entry points.
    export { addMarketplace } from "./add.ts";
    export { removeMarketplace } from "./remove.ts";
    export { listMarketplaces } from "./list.ts";
    export {
      updateAllMarketplaces,
      updateMarketplace,
    } from "./update.ts";
    export { setMarketplaceAutoupdate } from "./autoupdate.ts";

    export type { AddMarketplaceOptions } from "./add.ts";
    export type { RemoveMarketplaceOptions } from "./remove.ts";
    export type { ListMarketplacesOptions } from "./list.ts";
    export type {
      UpdateAllMarketplacesOptions,
      UpdateMarketplaceOptions,
    } from "./update.ts";
    export type { AutoupdateOptions } from "./autoupdate.ts";
    ```

    Notes:
    - This task runs AFTER all Wave 2 orchestrator files exist on disk -- depends_on includes 04-05..04-09.
    - The Wave 2 plans' `<action>` text instructs the executor to ALSO append re-exports to index.ts; that is a redundant per-plan instruction kept so each plan is independently complete (executors of single plans aren't blocked). When the full Phase 4 sequence runs, this Task 3 supersedes those per-plan appends -- overwrite the file with the consolidated barrel above. The redundancy is intentional: it keeps each Wave 2 plan readable in isolation.
  </action>
  <verify>
    <automated>test -f extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; grep -q "addMarketplace" extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; grep -q "removeMarketplace" extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; grep -q "listMarketplaces" extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; grep -q "updateMarketplace" extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; grep -q "updateAllMarketplaces" extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; grep -q "setMarketplaceAutoupdate" extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; grep -q "DEFAULT_GIT_OPS" extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; grep -q "cascadeUnstagePlugin" extensions/pi-claude-marketplace/orchestrators/marketplace/index.ts &amp;&amp; npm run check</automated>
  </verify>
  <done>
    - `orchestrators/marketplace/index.ts` re-exports all 6 entry-point function names (addMarketplace, removeMarketplace, listMarketplaces, updateMarketplace, updateAllMarketplaces, setMarketplaceAutoupdate)
    - Plus 5 cross-subcommand helpers (DEFAULT_GIT_OPS, applyAutoupdateFlip, cascadeUnstagePlugin, formatErrorWithCauses, resolveScopeFromState)
    - Plus 8 type re-exports
    - `npm run check` exits 0
  </done>
</task>

</tasks>

<verification>
- `npm run check` exits 0 (no test changes; just markdown edits -- typecheck and lint don't break)
- `grep -c "superseded by Phase 4 D-14" .planning/REQUIREMENTS.md` returns ≥3 (one in MU-2 list item, one in MU-3 list item, one in Coverage block)
- `grep "D-23 (2026-05-10)" .planning/PROJECT.md` returns 1 match
- The PRD file is unchanged: `git diff docs/prd/pi-claude-marketplace-prd.md` is empty
</verification>

<success_criteria>
- REQUIREMENTS.md MU-2 and MU-3 strikethrough + supersession notes added
- REQUIREMENTS.md Traceability table updated for both REQ-IDs
- REQUIREMENTS.md Coverage block + Phase 4 Per-phase counts row updated
- PROJECT.md Key Decisions table has new D-23 row
- PRD §5.1.4 not modified
- Mirrors the MA-7 / D-21 precedent set by Plan 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketplace-orchestrators/04-10-SUMMARY.md`. Include: the four exact strings introduced into REQUIREMENTS.md (the two `~~**MU-N**:...` strikethrough lines, the two Traceability rows, and the Phase 4 Per-phase counts row), confirmation that PROJECT.md gained a single new D-23 row, and confirmation that the PRD was NOT modified (`git diff docs/prd/pi-claude-marketplace-prd.md` is empty).
</output>
