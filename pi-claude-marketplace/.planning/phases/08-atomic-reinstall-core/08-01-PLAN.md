---
phase: 08-atomic-reinstall-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/transaction/with-state-guard.ts
  - tests/transaction/with-state-guard.test.ts
  - tests/architecture/no-orchestrator-network.test.ts
autonomous: true
requirements:
  - PRL-07
  - PRL-10
must_haves:
  truths:
    - "Reinstall can hold the existing per-scope .state-lock while explicitly saving state and reacting to save failures."
    - "The reinstall orchestrator path is architecture-gated against Git/network imports before it is implemented."
  artifacts:
    - path: "extensions/pi-claude-marketplace/transaction/with-state-guard.ts"
      provides: "Lock-held manual-save transaction helper for reinstall"
      exports:
        - "withLockedStateTransaction"
    - path: "tests/transaction/with-state-guard.test.ts"
      provides: "Lock-helper tests for held lock, explicit save, save failure, and release behavior"
    - path: "tests/architecture/no-orchestrator-network.test.ts"
      provides: "PRL-07 source-grep guard including reinstall.ts"
  key_links:
    - from: "extensions/pi-claude-marketplace/transaction/with-state-guard.ts"
      to: "extensions/pi-claude-marketplace/persistence/state-io.ts"
      via: "loadState/saveState imports and explicit transaction save()"
      pattern: "export async function withLockedStateTransaction"
    - from: "tests/architecture/no-orchestrator-network.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      via: "FORBIDDEN_TARGETS includes reinstall.ts"
      pattern: "orchestrators/plugin/reinstall.ts"
---

<objective>
Create the Phase 8 foundation: a lock-held manual-save transaction helper and a no-network architecture guard for the future reinstall orchestrator.

Purpose: Phase 8 needs rollback after physical replacement when `saveState` fails, which existing `withStateGuard` cannot expose because it auto-saves after the callback.
Output: transaction helper + tests; architecture guard extended for PRL-07.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-atomic-reinstall-core/08-CONTEXT.md
@.planning/phases/08-atomic-reinstall-core/08-RESEARCH.md
@.planning/phases/08-atomic-reinstall-core/08-VALIDATION.md
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@tests/transaction/with-state-guard.test.ts
@tests/architecture/no-orchestrator-network.test.ts

<interfaces>
Existing contracts to preserve:
```typescript
export async function withStateGuard<T>(
  locations: ScopedLocations,
  mutate: (state: ExtensionState) => Promise<T> | T,
): Promise<T>;

export async function loadState(extensionRoot: string): Promise<ExtensionState>;
export async function saveState(extensionRoot: string, state: ExtensionState): Promise<void>;
```

New helper contract to add:
```typescript
export interface LockedStateTransaction {
  readonly state: ExtensionState;
  save(): Promise<void>;
}

export interface LockedStateTransactionDeps {
  readonly loadState?: typeof loadState;
  readonly saveState?: typeof saveState;
}

export async function withLockedStateTransaction<T>(
  locations: ScopedLocations,
  run: (tx: LockedStateTransaction) => Promise<T> | T,
  deps?: LockedStateTransactionDeps,
): Promise<T>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lock-held manual-save transaction helper</name>
  <files>extensions/pi-claude-marketplace/transaction/with-state-guard.ts, tests/transaction/with-state-guard.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/transaction/with-state-guard.ts
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - tests/transaction/with-state-guard.test.ts
    - .planning/phases/08-atomic-reinstall-core/08-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `withLockedStateTransaction` exposes a fresh mutable `tx.state`; state is written only after `await tx.save()`.
    - Test 2: while the callback is running, `proper-lockfile.check(loc.extensionRoot, { lockfilePath: loc.stateLockFile, realpath: false })` returns `true`.
    - Test 3: if `tx.save()` throws using injected `deps.saveState`, the lock is released and a subsequent `withStateGuard` call succeeds.
    - Test 4: if the callback throws before `tx.save()`, `state.json` remains unchanged and the lock is released.
  </behavior>
  <action>
    Add `LockedStateTransaction`, `LockedStateTransactionDeps`, and `withLockedStateTransaction` to `extensions/pi-claude-marketplace/transaction/with-state-guard.ts` per D-04 and D-05.

    Implementation details:
    - Reuse the exact lock acquisition options already used by `withStateGuard`: `lockfile.lock(locations.extensionRoot, { lockfilePath: locations.stateLockFile, realpath: false, retries: 0, stale: 10_000, update: 2_000 })`.
    - Reuse the same `mkdir(locations.extensionRoot, { recursive: true })`, `isLockHeldError`, `StateLockHeldError`, and `toError` behavior.
    - Load state with `const fresh = await (deps?.loadState ?? loadState)(locations.extensionRoot)` before invoking `run`.
    - Pass `run` an object `{ state: fresh, save: async () => { await (deps?.saveState ?? saveState)(locations.extensionRoot, fresh); } }`.
    - Do not auto-save after `run` returns. The caller controls when/if `save()` is invoked.
    - Always release the lock in `finally`; if release fails and there is no primary error, throw the release error just like `withStateGuard`.
    - Preserve the existing `withStateGuard` export and behavior unchanged.

    Extend `tests/transaction/with-state-guard.test.ts` with focused tests named with `PRL-10` or `Phase 8` that assert the four behavior bullets above.
  </action>
  <verify>
    <automated>node --test tests/transaction/with-state-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/transaction/with-state-guard.ts` contains `export async function withLockedStateTransaction`.
    - `extensions/pi-claude-marketplace/transaction/with-state-guard.ts` contains `readonly saveState?: typeof saveState`.
    - `tests/transaction/with-state-guard.test.ts` contains `withLockedStateTransaction`.
    - `node --test tests/transaction/with-state-guard.test.ts` exits 0.
  </acceptance_criteria>
  <done>Manual-save helper exists, is tested, and does not alter existing withStateGuard behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend no-network architecture guard for reinstall</name>
  <files>tests/architecture/no-orchestrator-network.test.ts</files>
  <read_first>
    - tests/architecture/no-orchestrator-network.test.ts
    - .planning/phases/08-atomic-reinstall-core/08-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Test 1: `FORBIDDEN_TARGETS` includes `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts`.
    - Test 2: forbidden patterns include direct `refreshGitHubClone` references in addition to `platform/git`, `DEFAULT_GIT_OPS`, and `gitOps`.
    - Test 3: the test continues to skip ENOENT targets so Plan 01 can land before `reinstall.ts` exists.
  </behavior>
  <action>
    Update `tests/architecture/no-orchestrator-network.test.ts` for PRL-07 and D-03.

    Concrete changes:
    - Add `"extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"` to `FORBIDDEN_TARGETS`.
    - Add `{ name: "refreshGitHubClone reference", pattern: /\brefreshGitHubClone\b/ }` to `FORBIDDEN_PATTERNS`.
    - Update the header comment to say install, list, and reinstall are network-free by contract, while update remains exempt.
    - Preserve `stripComments(src)` before pattern checks so comments can document forbidden strings.
  </action>
  <verify>
    <automated>node --test tests/architecture/no-orchestrator-network.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/architecture/no-orchestrator-network.test.ts` contains `orchestrators/plugin/reinstall.ts`.
    - `tests/architecture/no-orchestrator-network.test.ts` contains `refreshGitHubClone reference`.
    - `node --test tests/architecture/no-orchestrator-network.test.ts` exits 0 before `reinstall.ts` exists.
  </acceptance_criteria>
  <done>PRL-07 guard is in place before production reinstall implementation.</done>
</task>

</tasks>

<verification>
Run:
```bash
node --test tests/transaction/with-state-guard.test.ts tests/architecture/no-orchestrator-network.test.ts
```
Then ensure TypeScript still accepts the new exported helper when downstream plans import it.
</verification>

<success_criteria>
- `withLockedStateTransaction` exists and holds the same lock as `withStateGuard`.
- State is saved only when caller invokes `tx.save()`.
- Lock release is proven after callback and save failures.
- The architecture no-network guard includes reinstall and `refreshGitHubClone`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-atomic-reinstall-core/08-01-SUMMARY.md`.
</output>
