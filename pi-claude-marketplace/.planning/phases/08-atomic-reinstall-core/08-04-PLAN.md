---
phase: 08-atomic-reinstall-core
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/index.ts
  - extensions/pi-claude-marketplace/orchestrators/index.ts
  - extensions/pi-claude-marketplace/orchestrators/types.ts
  - tests/orchestrators/plugin/reinstall.test.ts
autonomous: true
requirements:
  - PRL-02
  - PRL-06
  - PRL-07
  - PRL-08
  - PRL-09
  - PRL-10
  - PRL-11
  - PRL-12
must_haves:
  truths:
    - "A caller can reinstall one already-installed plugin from the cached manifest without Git/network operations."
    - "Absent installed records produce a non-mutating skipped/not-installed core outcome."
    - "Installed records with bad cached manifest/restage inputs fail while preserving old state, old resources, agents index, MCP entries, and plugin data."
    - "Successful reinstall preserves the old installed version, restages resources, updates resource lists, and deletes plugin data after state commit."
    - "Plugin data cleanup failure emits a warning and does not convert successful reinstall into failure."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      provides: "Single-plugin atomic reinstall core"
      exports:
        - "reinstallPlugin"
        - "ReinstallPluginOptions"
        - "ReinstallPluginOutcome"
    - path: "tests/orchestrators/plugin/reinstall.test.ts"
      provides: "PRL-02/06/07/08/09/10/11/12 focused orchestrator coverage"
    - path: "extensions/pi-claude-marketplace/orchestrators/types.ts"
      provides: "Shared reinstall outcome/partition types if promoted beyond plugin barrel"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      to: "extensions/pi-claude-marketplace/transaction/with-state-guard.ts"
      via: "withLockedStateTransaction holds .state-lock across prepare, replacement, save, rollback"
      pattern: "withLockedStateTransaction"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      to: "extensions/pi-claude-marketplace/bridges/*/stage.ts"
      via: "replacePrepared*, rollback*Replacement, finalize*Replacement helpers"
      pattern: "rollback.*Replacement"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/index.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      via: "barrel export for Phase 9 edge wiring"
      pattern: "reinstallPlugin"
---

<objective>
Implement the single-plugin atomic reinstall core.

Purpose: Phase 8 culminates in an orchestrator that can restage one installed plugin from cached state while preserving the old install on every failure path and deleting plugin data only after success.
Output: `reinstallPlugin` core API, exported outcome types, and full orchestrator tests.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-atomic-reinstall-core/08-CONTEXT.md
@.planning/phases/08-atomic-reinstall-core/08-RESEARCH.md
@.planning/phases/08-atomic-reinstall-core/08-VALIDATION.md
@.planning/phases/08-atomic-reinstall-core/08-01-SUMMARY.md
@.planning/phases/08-atomic-reinstall-core/08-02-SUMMARY.md
@.planning/phases/08-atomic-reinstall-core/08-03-SUMMARY.md
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/index.ts
@extensions/pi-claude-marketplace/orchestrators/index.ts
@extensions/pi-claude-marketplace/orchestrators/types.ts
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/shared/markers.ts
@extensions/pi-claude-marketplace/shared/completion-cache.ts
@extensions/pi-claude-marketplace/presentation/reload-hint.ts
@extensions/pi-claude-marketplace/presentation/soft-dep.ts
@tests/orchestrators/plugin/install.test.ts
@tests/orchestrators/plugin/update.test.ts
@tests/orchestrators/plugin/uninstall.test.ts
@tests/architecture/no-orchestrator-network.test.ts

<interfaces>
Required public core API:
```typescript
export interface ReinstallPluginOptions {
  readonly ctx: ExtensionContext;
  readonly pi: ExtensionAPI;
  readonly scope: Scope;
  readonly cwd: string;
  readonly marketplace: string;
  readonly plugin: string;
  readonly force?: boolean;
}

export type ReinstallPluginPartition = "reinstalled" | "skipped" | "failed";

export interface ReinstallPluginOutcome {
  readonly partition: ReinstallPluginPartition;
  readonly name: string;
  readonly marketplace: string;
  readonly scope: Scope;
  readonly version?: string;
  readonly notes?: readonly string[];
  readonly stagedAgents?: readonly string[];
  readonly stagedMcpServers?: readonly string[];
  readonly resourcesChanged?: boolean;
}

export async function reinstallPlugin(opts: ReinstallPluginOptions): Promise<ReinstallPluginOutcome>;
```

If you promote outcome types into `orchestrators/types.ts`, keep the plugin barrel and top-level orchestrator barrel exporting them for Phase 9.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reinstall orchestrator API and preflight</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/index.ts, extensions/pi-claude-marketplace/orchestrators/index.ts, extensions/pi-claude-marketplace/orchestrators/types.ts, tests/orchestrators/plugin/reinstall.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
    - extensions/pi-claude-marketplace/transaction/with-state-guard.ts
    - extensions/pi-claude-marketplace/domain/manifest.ts
    - extensions/pi-claude-marketplace/domain/components/plugin.ts
    - extensions/pi-claude-marketplace/domain/resolver.ts
    - tests/orchestrators/plugin/install.test.ts
    - tests/orchestrators/plugin/update.test.ts
    - .planning/phases/08-atomic-reinstall-core/08-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: when marketplace/plugin installed record is absent, `reinstallPlugin` returns `{ partition: "skipped", notes: ["not installed"] }` or equivalent, sends no success reload hint, and does not mutate `state.json` or disk.
    - Test 2: when installed record exists but manifest entry is missing/schema-invalid/not installable, `reinstallPlugin` returns `{ partition: "failed" }` or emits `notifyError`, and old state/resources/data remain unchanged.
    - Test 3: when manifest entry version differs from installed record version, success keeps `state.marketplaces[mp].plugins[plugin].version` equal to the old installed version.
    - Test 4: `tests/architecture/no-orchestrator-network.test.ts` fails if `reinstall.ts` references `gitOps`, `DEFAULT_GIT_OPS`, `refreshGitHubClone`, or `platform/git` outside comments.
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` with the public API shown in the interfaces block, per D-01 through D-03, D-15, and PRL-02/06/07/08.

    Concrete preflight flow inside `withLockedStateTransaction(locations, async (tx) => { ... })`:
    - Resolve `const locations = locationsFor(scope, cwd)`.
    - Read fresh state from `tx.state`; do not call `loadState` separately outside the lock.
    - If `state.marketplaces[marketplace]` is absent or `mp.plugins[plugin]` is absent, return a skipped/not-installed outcome and perform no disk mutation.
    - Save a deep snapshot of the old installed record fields needed for assertions: `version`, `installedAt`, `resources`, `resolvedSource`, `compatibility`, and `updatedAt`.
    - Load cached manifest with `loadMarketplaceManifest(mp.manifestPath)` only. Do not import or call any Git/network helper.
    - Find the plugin entry by exact `entry.name === plugin`.
    - If the entry is absent, fails `PLUGIN_ENTRY_VALIDATOR.Check(entryRaw)`, `resolveStrict(entry, { marketplaceRoot: mp.marketplaceRoot })` throws, or `requireInstallable(resolved, "reinstall")` throws, return/throw a failed outcome after `notifyError(ctx, formatErrorWithCauses(err), err)` and leave state/disk/data untouched.
    - Do not call `resolvePluginVersion` or `computeHashVersion`; set the post-reinstall version to `oldRecord.version`.
    - Discover generated skill/command/agent names using the same `discoverPluginSkills`, `discoverPluginCommands`, `pickAgentsSourceDir`, and `discoverPluginAgents` pattern from install/update.
    - Run `assertNoCrossPluginConflicts(scope, generatedNames, stateWithoutThisPlugin)` where `stateWithoutThisPlugin` excludes only `state.marketplaces[marketplace].plugins[plugin]` per D-15. You may factor a local `removePluginRecordForGuard` helper like update's `removePluginRecord`.
    - Prepare all bridge handles before any replacement: skills, commands, agents, MCP. Pass previous resources from old record into skills/commands prepare; agents/MCP derive previous state from their bridge indexes/docs.
    - For agents prepare, pass `knownSkills` equal to the newly prepared skill names so skills references validate like install.
    - If any prepare fails, abort all already-prepared handles in reverse order and preserve old state/resources/data.
    - Export the new API from `orchestrators/plugin/index.ts` and `orchestrators/index.ts`. If outcome types live in `orchestrators/types.ts`, export them there too.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/reinstall.test.ts tests/architecture/no-orchestrator-network.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `export async function reinstallPlugin`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` does not contain `resolvePluginVersion`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` does not contain `DEFAULT_GIT_OPS`, `refreshGitHubClone`, or `platform/git` outside comments.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/index.ts` exports `reinstallPlugin`.
    - `node --test tests/orchestrators/plugin/reinstall.test.ts tests/architecture/no-orchestrator-network.test.ts` exits 0.
  </acceptance_criteria>
  <done>Reinstall core API exists with installed-only, cached-manifest, version-preserving preflight semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic replacement, state save, and rollback</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, tests/orchestrators/plugin/reinstall.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/transaction/with-state-guard.ts
    - extensions/pi-claude-marketplace/bridges/skills/stage.ts
    - extensions/pi-claude-marketplace/bridges/commands/stage.ts
    - extensions/pi-claude-marketplace/bridges/agents/stage.ts
    - extensions/pi-claude-marketplace/bridges/mcp/stage.ts
    - extensions/pi-claude-marketplace/shared/markers.ts
    - tests/orchestrators/plugin/reinstall.test.ts
    - .planning/phases/08-atomic-reinstall-core/08-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: successful reinstall restages skill/command/agent/MCP resources and updates `resources.{skills,prompts,agents,mcpServers}` while preserving `version` and `installedAt` and updating `updatedAt`.
    - Test 2: injected skills/commands/agents/MCP replacement failure leaves old resource bytes, old agents-index, old mcp.json, old state, and plugin data dir intact.
    - Test 3: injected `tx.save()` failure after physical replacement rolls back all replaced bridges in reverse order `mcp -> agents -> commands -> skills`, leaves old state on disk, and preserves plugin data dir.
    - Test 4: if rollback itself fails, the user-visible error contains `MANUAL RECOVERY REQUIRED:` and names the failing bridge phase/path.
    - Test 5: default foreign previous agent content fails before replacement; `force: true` replaces it and rollback restores it on later failure.
  </behavior>
  <action>
    Complete the atomic transaction body in `reinstallPlugin` per D-04 through D-07 and D-11 through D-16.

    Concrete replacement flow:
    - After all prepares succeed, call replacement helpers in this order: `replacePreparedSkills`, `replacePreparedCommands`, `replacePreparedAgents`, `replacePreparedMcp`.
    - Pass `{ force }` to `replacePreparedAgents`.
    - Track successful replacement handles in a local stack with phase names exactly `skills`, `commands`, `agents`, `mcp`.
    - If a replacement helper throws, rollback already replaced handles in reverse order using `rollbackMcpReplacement`, `rollbackAgentsReplacement`, `rollbackCommandsReplacement`, `rollbackSkillsReplacement`, then abort any prepared-but-not-replaced staging handles. Return/notify failed outcome.
    - After physical replacement succeeds, mutate `tx.state.marketplaces[marketplace].plugins[plugin]` in place:
      - `version: oldRecord.version`
      - `resolvedSource: installable.pluginRoot`
      - `compatibility.installable: true`
      - `compatibility.notes/supported/unsupported` copied from `installable`
      - `resources.skills` from `skillsPrep.result.recorded.map(r => r.generatedName)`
      - `resources.prompts` from `commandsPrep.result.recorded.map(r => r.generatedName)`
      - `resources.agents` from `agentsPrep.result.recorded.map(r => r.generatedName)`
      - `resources.mcpServers` from `mcpPrep.result.recorded.map(r => r.generatedName)`
      - `installedAt: oldRecord.installedAt`
      - `updatedAt: new Date().toISOString()`
    - Call `await tx.save()` explicitly.
    - If `tx.save()` throws, rollback all replacements in reverse order while state on disk remains old, then throw/return failed outcome.
    - After successful save, call finalize helpers in replacement order or reverse order consistently; collect cleanup leak strings and route them to `notifyWarning(ctx, leak)` without failing reinstall.
    - Compose rollback failure errors with `MANUAL_RECOVERY_REQUIRED` imported from `shared/markers.ts`; include bridge phase and exact failed path/detail. Do not introduce a reinstall-specific stable marker.
    - Keep all user-visible errors/warnings through `notifyError`/`notifyWarning`; no stdout/stderr writes.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/reinstall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `replacePreparedSkills`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `rollbackMcpReplacement`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `await tx.save()`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `installedAt: oldRecord.installedAt` or equivalent preservation.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `version: oldRecord.version` or equivalent preservation.
    - `tests/orchestrators/plugin/reinstall.test.ts` contains `saveState failure` or `save failure`.
    - `node --test tests/orchestrators/plugin/reinstall.test.ts` exits 0.
  </acceptance_criteria>
  <done>Reinstall replacement is rollback-protected across bridge and state-save failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add post-success cache, data cleanup, notifications, and final exports</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/index.ts, extensions/pi-claude-marketplace/orchestrators/index.ts, tests/orchestrators/plugin/reinstall.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    - extensions/pi-claude-marketplace/presentation/reload-hint.ts
    - extensions/pi-claude-marketplace/presentation/soft-dep.ts
    - extensions/pi-claude-marketplace/shared/completion-cache.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/orchestrators/plugin/reinstall.test.ts
    - .planning/phases/08-atomic-reinstall-core/08-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: successful reinstall with at least one generated resource emits success with `Run /reload to refresh it.` or existing `refresh` reload hint wording.
    - Test 2: successful reinstall with no generated resources suppresses reload hint.
    - Test 3: successful reinstall with staged agents/MCP emits existing pi-subagents/pi-mcp-adapter soft-dependency warnings when the relevant tool is unloaded.
    - Test 4: completion cache invalidation failure is warning-only after success.
    - Test 5: plugin data directory is deleted after successful state commit and left absent.
    - Test 6: plugin data cleanup failure emits warning severity and the outcome remains `partition: "reinstalled"`.
  </behavior>
  <action>
    Add the post-success tail to `reinstallPlugin` per D-08 through D-10 and PRL-11/12.

    Concrete success flow after `withLockedStateTransaction` returns successfully:
    - Drop the marketplace completion cache with `dropMarketplaceCache(await locations.pluginCacheFile(marketplace), scope, marketplace)` inside try/catch. On failure call `notifyWarning(ctx, 'Plugin "' + plugin + '" reinstalled; completion cache refresh deferred: ' + errorMessage(err))` or equivalent warning text.
    - Resolve `const dataDir = await locations.pluginDataDir(marketplace, plugin)` and delete it with `await rm(dataDir, { recursive: true, force: true })`.
    - If data-dir cleanup fails, call `notifyWarning(ctx, 'Plugin "' + plugin + '" reinstalled; data cleanup deferred at ' + dataDir + ': ' + errorMessage(err))` or equivalent. Do not change the success outcome to failed.
    - Do not recreate an empty data directory after cleanup.
    - Compute `resourcesChanged` as true when any staged skill, prompt, agent, or MCP server name exists, or when old resource lists differ from new resource lists. Use this to decide whether `reloadHint("refresh", [plugin])` is appended.
    - Use `subagentWarningIfNeeded(pi, stagedAgentNames)` and `mcpAdapterWarningIfNeeded(pi, stagedMcpServerNames)` for soft-dependency warnings.
    - Send a success notification through `notifySuccess(ctx, appendReloadHint(body, hint))` for direct core use. Body should include `Reinstalled plugin "${plugin}" from marketplace "${marketplace}".` or a consistent existing-lifecycle sentence.
    - Return a `ReinstallPluginOutcome` with `partition: "reinstalled"`, `name`, `marketplace`, `scope`, `version: oldRecord.version`, `stagedAgents`, `stagedMcpServers`, and `resourcesChanged`.
    - Ensure skipped/not-installed outcomes do not emit reload hints.
    - Ensure failed outcomes/errors preserve old plugin data dir.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/reinstall.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `dropMarketplaceCache`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `rm(dataDir, { recursive: true, force: true })` or equivalent data-dir cleanup.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` does not contain `mkdir(dataDir`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `reloadHint("refresh"`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains `subagentWarningIfNeeded` and `mcpAdapterWarningIfNeeded`.
    - `node --test tests/orchestrators/plugin/reinstall.test.ts` exits 0.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Successful reinstall performs warning-only post-success maintenance and exposes complete Phase 9-ready outcome fields.</done>
</task>

</tasks>

<verification>
Run focused checks:
```bash
node --test tests/orchestrators/plugin/reinstall.test.ts tests/architecture/no-orchestrator-network.test.ts
```
Then run phase-relevant suites:
```bash
node --test "tests/{architecture,bridges,orchestrators,transaction}/**/*.test.ts"
```
Finally run:
```bash
npm run check
```
</verification>

<success_criteria>
- PRL-02 direct single-plugin reinstall core exists.
- PRL-06 absent installed records are non-mutating skipped outcomes.
- PRL-07 no-network architecture guard passes against reinstall.ts.
- PRL-08 state version is preserved from old installed record.
- PRL-09 all resources are prepared before replacement.
- PRL-10 preflight, prepare, replacement, and state-save failures preserve old state/resources/data.
- PRL-11 data dir is deleted only after successful replacement and state commit.
- PRL-12 data cleanup failure is warning-only.
</success_criteria>

<output>
After completion, create `.planning/phases/08-atomic-reinstall-core/08-04-SUMMARY.md`.
</output>
