---
phase: 10-claude-settings-import-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/import/types.ts
  - extensions/pi-claude-marketplace/orchestrators/import/settings.ts
  - extensions/pi-claude-marketplace/orchestrators/import/index.ts
  - extensions/pi-claude-marketplace/orchestrators/index.ts
  - tests/orchestrators/import/settings.test.ts
autonomous: true
requirements: [IMP-04]
requirements_addressed: [IMP-04]
must_haves:
  truths:
    - "Claude settings paths resolve exactly per D-01 for user default, user CLAUDE_CONFIG_DIR override, and project scope."
    - "Missing settings files become empty settings, while malformed JSON becomes a diagnostic and does not crash the import foundation."
    - "Local settings shallow-merge known sections over base settings by key."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/import/types.ts"
      provides: "Shared Phase 10 import diagnostic and merged-settings types"
    - path: "extensions/pi-claude-marketplace/orchestrators/import/settings.ts"
      provides: "Claude settings path discovery, optional JSON read, and known-section merge helpers"
    - path: "tests/orchestrators/import/settings.test.ts"
      provides: "IMP-04 regression coverage"
  key_links:
    - from: "Phase 11 import orchestrator"
      to: "loadMergedClaudeSettingsForScope"
      via: "orchestrators/import/index.ts barrel export"
---

<objective>
Create the settings discovery and merge foundation for Claude Code import.

Purpose: IMP-04 requires import to read `settings.json` plus `settings.local.json` for each selected Claude scope, with deterministic local-over-base semantics. Phase 10 must expose this as pure/testable helpers and diagnostics, without mutating Pi state or notifying users directly.
Output: typed import settings/diagnostic primitives, path resolver seam, optional JSON reader, known-section merge helper, and unit tests.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-claude-settings-import-foundation/10-CONTEXT.md
@.planning/phases/10-claude-settings-import-foundation/10-RESEARCH.md
@.planning/phases/10-claude-settings-import-foundation/10-VALIDATION.md
@extensions/pi-claude-marketplace/shared/types.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/orchestrators/index.ts
@tests/persistence/state-io.test.ts

<interfaces>
Create an `orchestrators/import/` module whose public barrel exports at least: `ClaudeSettingsPaths`, `ClaudeSettingsReadOptions`, `ImportDiagnostic`, `MergedClaudeSettings`, `resolveClaudeSettingsPaths`, `loadMergedClaudeSettingsForScope`, and `mergeClaudeSettings`. Names may be adjusted only if the same semantics are exported and tests assert the public behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add import foundation types and settings path resolver</name>
  <files>extensions/pi-claude-marketplace/orchestrators/import/types.ts, extensions/pi-claude-marketplace/orchestrators/import/settings.ts, extensions/pi-claude-marketplace/orchestrators/import/index.ts, extensions/pi-claude-marketplace/orchestrators/index.ts, tests/orchestrators/import/settings.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/shared/types.ts; extensions/pi-claude-marketplace/persistence/locations.ts; extensions/pi-claude-marketplace/orchestrators/index.ts; .planning/phases/10-claude-settings-import-foundation/10-CONTEXT.md decisions D-01 through D-03; .planning/phases/10-claude-settings-import-foundation/10-RESEARCH.md Architecture Patterns</read_first>
  <action>Create `extensions/pi-claude-marketplace/orchestrators/import/` with a barrel `index.ts`, `types.ts`, and `settings.ts`. Define `ImportDiagnostic` with fields `severity: "warning" | "error"`, `scope: Scope`, `code`, `message`, and optional `path`, `ref`, `marketplace`. Define `ClaudeSettingsPaths` with `basePath` and `localPath`. Implement `resolveClaudeSettingsPaths(scope, options)` so `scope === "user"` resolves `{basePath, localPath}` to `${claudeConfigDir ?? path.join(os.homedir(), ".claude")}/settings.json` and `settings.local.json`, where `claudeConfigDir` is an explicit option defaulting to `process.env.CLAUDE_CONFIG_DIR`; `scope === "project"` resolves to `${cwd}/.claude/settings.json` and `${cwd}/.claude/settings.local.json`. Add an `orchestrators/import/index.ts` export and re-export that namespace from `extensions/pi-claude-marketplace/orchestrators/index.ts`. Do not introduce any scope other than `Scope = "user" | "project"`; do not read files in the path resolver.</action>
  <verify>
    <automated>npm test -- tests/orchestrators/import/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/orchestrators/import/settings.test.ts` asserts default user paths contain `.claude/settings.json` and `.claude/settings.local.json` under a controlled home/config option.
    - `tests/orchestrators/import/settings.test.ts` asserts user `claudeConfigDir: "/tmp/claude-config"` produces `/tmp/claude-config/settings.json` and `/tmp/claude-config/settings.local.json`.
    - `tests/orchestrators/import/settings.test.ts` asserts project `cwd: "/repo"` produces `/repo/.claude/settings.json` and `/repo/.claude/settings.local.json`.
    - Source assertion: no exported type or function mentions a `local` Pi scope.
  </acceptance_criteria>
  <done>Phase 10 has a public import-foundation module and deterministic settings path resolver for user and project scopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement optional settings JSON reads with diagnostics</name>
  <files>extensions/pi-claude-marketplace/orchestrators/import/types.ts, extensions/pi-claude-marketplace/orchestrators/import/settings.ts, tests/orchestrators/import/settings.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/persistence/state-io.ts; tests/persistence/state-io.test.ts; .planning/phases/10-claude-settings-import-foundation/10-CONTEXT.md decisions D-02, D-07, D-08; .planning/phases/10-claude-settings-import-foundation/10-RESEARCH.md Pattern 1</read_first>
  <action>Implement `readClaudeSettingsFile(scope, filePath, kind, options?)` or an equivalent private helper used by `loadMergedClaudeSettingsForScope`. Missing files (`ENOENT`) must return an empty object and no diagnostic per D-02. Malformed JSON must return an empty object plus one diagnostic with `code: "malformed-json"`, `severity: "warning"`, the current `scope`, and `path` equal to the malformed file path per D-08. Other file-read failures should return an empty object plus a diagnostic code such as `settings-read-error` with enough message detail for Phase 11 warning presentation. Do not call `ctx.ui.notify`, `console.log`, `process.stdout`, marketplace add, or plugin install.</action>
  <verify>
    <automated>npm test -- tests/orchestrators/import/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: missing `settings.json` and missing `settings.local.json` produce `settings.enabledPlugins` as `{}`, `settings.extraKnownMarketplaces` as `{}`, and `diagnostics.length === 0`.
    - Test assertion: malformed base JSON records exactly one diagnostic whose `code` is `malformed-json`, whose `path` is the base settings file, and still allows a valid local settings file to contribute entries.
    - Test assertion: malformed local JSON records exactly one diagnostic while valid base settings still contributes entries.
    - Source assertion: `settings.ts` contains no `ctx.ui.notify`, `console.`, `process.stdout`, or `process.stderr` calls.
  </acceptance_criteria>
  <done>Settings file loading is warn-and-continue and returns diagnostics as data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement known-section shallow merge with local override</name>
  <files>extensions/pi-claude-marketplace/orchestrators/import/types.ts, extensions/pi-claude-marketplace/orchestrators/import/settings.ts, tests/orchestrators/import/settings.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/import/settings.ts; .planning/phases/10-claude-settings-import-foundation/10-CONTEXT.md decisions D-04 through D-06; .planning/phases/10-claude-settings-import-foundation/10-RESEARCH.md Pattern 2</read_first>
  <action>Implement `mergeClaudeSettings(base, local)` to extract only plain-object `enabledPlugins` and `extraKnownMarketplaces`, shallow-merge those sections, and return `MergedClaudeSettings` with exactly `enabledPlugins: Record<string, unknown>` and `extraKnownMarketplaces: Record<string, unknown>`. Use local-over-base by key for both sections. Do not deep-merge nested marketplace objects: if base has `extraKnownMarketplaces.private = { github: { repo: "old/repo" }, other: true }` and local has `extraKnownMarketplaces.private = { directory: "../local" }`, the merged `private` value must be exactly `{ directory: "../local" }`. Ensure `loadMergedClaudeSettingsForScope(scope, options)` combines path resolution, reads, merge, and diagnostics in one Phase 11-friendly result.</action>
  <verify>
    <automated>npm run typecheck && npm test -- tests/orchestrators/import/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: base `enabledPlugins["a@mp"] = true` plus local `enabledPlugins["a@mp"] = false` yields merged value `false`.
    - Test assertion: unrelated base and local `enabledPlugins` keys are both preserved.
    - Test assertion: local `extraKnownMarketplaces.private` replaces, not deep-merges, base `extraKnownMarketplaces.private`.
    - Test assertion: non-object known sections are treated as empty and do not throw.
    - CLI assertion: `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>IMP-04 merge semantics are implemented and covered by deterministic unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude settings files -> import foundation | User-controlled JSON enters pure TypeScript helpers. |
| Import foundation -> Phase 11 command handler | Diagnostics and merged settings flow to user-visible orchestration later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | settings JSON | mitigate | Treat malformed JSON and non-object sections as diagnostics/empty data instead of executing or trusting shape. |
| T-10-02 | Repudiation | diagnostics | mitigate | Include scope and path on malformed/read-error diagnostics so Phase 11 can present traceable warnings. |
| T-10-03 | Elevation of Privilege | scope handling | mitigate | Use existing `Scope` union only; path resolver has no `local` branch. |
</threat_model>

<verification>
Run `npm test -- tests/orchestrators/import/settings.test.ts`, then `npm run typecheck` after all tasks in this plan.
</verification>

<success_criteria>
`loadMergedClaudeSettingsForScope` can resolve, read, and merge user/project Claude settings with local override and diagnostics, satisfying IMP-04 without side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/10-claude-settings-import-foundation/10-01-SUMMARY.md`.
</output>
