---
phase: 09-reinstall-edge-bulk-ux
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/edge/completions/provider.ts
  - extensions/pi-claude-marketplace/edge/completions/data.ts
  - tests/edge/completions/provider.test.ts
autonomous: true
requirements:
  - PRL-16
must_haves:
  truths:
    - "Top-level completions include reinstall with trailing-space completion behavior."
    - "Reinstall plugin-ref completions are installed-only like update and uninstall."
    - "Reinstall supports @marketplace completion for marketplace-wide reinstall targets."
    - "Reinstall exposes --force in flag completions without exposing it for install/update/uninstall/list."
    - "Completion positional extraction skips --force so reinstall --force <TAB> still completes installed refs."
    - "Existing per-marketplace manifest soft-fail behavior and state-error propagation remain unchanged."
  artifacts:
    - path: "extensions/pi-claude-marketplace/edge/completions/provider.ts"
      provides: "Top-level reinstall, ref completion branch, and --force flag completion"
    - path: "extensions/pi-claude-marketplace/edge/completions/data.ts"
      provides: "Reinstall installed-only plugin-ref completion mode and boolean flag positional skipping"
    - path: "tests/edge/completions/provider.test.ts"
      provides: "PRL-16 completion coverage"
  key_links:
    - from: "extensions/pi-claude-marketplace/edge/completions/provider.ts"
      to: "extensions/pi-claude-marketplace/edge/completions/data.ts"
      via: "getPluginRefCompletions('reinstall', ..., { allowMarketplaceOnly: true })"
      pattern: "reinstall"
---

# Implementation Plan

## Goal
Add reinstall tab completion for top-level commands, installed plugin refs, marketplace-only targets, `--force`, and trailing-space behavior while preserving existing failure semantics.

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reinstall completion mode and --force positional skipping</name>
  <files>extensions/pi-claude-marketplace/edge/completions/data.ts, tests/edge/completions/provider.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/completions/data.ts
    - extensions/pi-claude-marketplace/edge/completions/provider.ts
    - tests/edge/completions/provider.test.ts
    - .planning/phases/09-reinstall-edge-bulk-ux/09-CONTEXT.md
    - .planning/phases/09-reinstall-edge-bulk-ux/09-RESEARCH.md
  </read_first>
  <action>
    Update `extensions/pi-claude-marketplace/edge/completions/data.ts`.

    Concrete changes:
    - Change `type PluginRefCompletionMode = "install" | "uninstall" | "update";` to `type PluginRefCompletionMode = "install" | "uninstall" | "update" | "reinstall";`.
    - Update the D-03 corollary comment so it includes `- mode = "reinstall" -> keep status === "installed".`
    - In `statusMatchesMode`, handle reinstall exactly like uninstall/update:
      ```typescript
      case "uninstall":
      case "update":
      case "reinstall":
        return row.status === "installed";
      ```
    - Replace `extractPositionals(tokens: readonly string[]): string[]` with `extractPositionals(tokens: readonly string[], booleanFlags: readonly string[] = []): string[]`.
    - Preserve existing `--scope <value>` skipping exactly.
    - Add boolean-flag skipping: if `booleanFlags.includes(t)`, increment `i` by 1 and continue without adding the token to positionals.
    - Do not skip arbitrary `--*` flags by default because list filters still need existing behavior unless the caller passes a flag list.

    Extend `tests/edge/completions/provider.test.ts` with direct or provider-level assertions named `PRL-16 :: reinstall completion mode shows only installed plugins` and `PRL-16 :: reinstall --force completion still reaches installed refs`.
  </action>
  <verify>
    <automated>node --test tests/edge/completions/provider.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/edge/completions/data.ts` contains `"install" | "uninstall" | "update" | "reinstall"`.
    - `extensions/pi-claude-marketplace/edge/completions/data.ts` contains `case "reinstall":`.
    - `extensions/pi-claude-marketplace/edge/completions/data.ts` contains `booleanFlags: readonly string[] = []`.
    - `extensions/pi-claude-marketplace/edge/completions/data.ts` contains `booleanFlags.includes`.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16 :: reinstall completion mode shows only installed plugins`.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16 :: reinstall --force completion still reaches installed refs`.
    - `node --test tests/edge/completions/provider.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add provider branches for reinstall refs and --force flags</name>
  <files>extensions/pi-claude-marketplace/edge/completions/provider.ts, tests/edge/completions/provider.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/completions/provider.ts
    - extensions/pi-claude-marketplace/edge/completions/data.ts
    - tests/edge/completions/provider.test.ts
    - .planning/phases/09-reinstall-edge-bulk-ux/09-CONTEXT.md
    - .planning/phases/09-reinstall-edge-bulk-ux/09-RESEARCH.md
  </read_first>
  <action>
    Update `extensions/pi-claude-marketplace/edge/completions/provider.ts`.

    Concrete changes:
    - Add `"reinstall"` to `TOP_LEVEL_SUBCOMMANDS` immediately after `"update"`.
    - Update the Branch 1 header comment to include `reinstall`.
    - Update the Branch 4 comment to say `install / uninstall / update / reinstall`.
    - Compute positionals with reinstall boolean flag skipping:
      ```typescript
      const rawHead = tokens.find((token) => token !== "--scope") ?? "";
      const positionals = extractPositionals(
        tokens,
        rawHead === "reinstall" ? ["--force"] : [],
      );
      ```
      If this exact implementation does not fit the final parser, the required behavior is that `extractPositionals(["reinstall", "--force"], ["--force"])` yields `["reinstall"]` and existing `extractPositionals(["list", "--installed"])` behavior is unchanged.
    - In `flagCompletions`, add `--force` only when `positionalHead === "reinstall"`:
      ```typescript
      if (positionalHead === "reinstall") {
        flags.push({ name: "--force", description: "Overwrite this plugin's previous foreign agent content" });
      }
      ```
    - Add a branch after the update branch:
      ```typescript
      if (positionalHead === "reinstall" && positionals.length === 1) {
        return getPluginRefCompletions("reinstall", current, argumentTextPrefix, resolver, {
          allowMarketplaceOnly: true,
        });
      }
      ```
    - Do not add `--force` for `install`, `uninstall`, `update`, `list`, `ls`, or `marketplace` heads.

    Extend `tests/edge/completions/provider.test.ts` with assertions named:
    - `PRL-16 :: top-level rei completes to reinstall with trailing space`
    - `PRL-16 :: reinstall @ completes marketplace-only targets`
    - `PRL-16 :: reinstall plugin half preserves multi-marketplace no trailing space`
    - `PRL-16 :: reinstall flag completion includes --force only for reinstall`
  </action>
  <verify>
    <automated>node --test tests/edge/completions/provider.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/edge/completions/provider.ts` contains `"reinstall"` in `TOP_LEVEL_SUBCOMMANDS`.
    - `extensions/pi-claude-marketplace/edge/completions/provider.ts` contains `{ name: "--force", description: "Overwrite this plugin's previous foreign agent content" }`.
    - `extensions/pi-claude-marketplace/edge/completions/provider.ts` contains `getPluginRefCompletions("reinstall"`.
    - `extensions/pi-claude-marketplace/edge/completions/provider.ts` contains `allowMarketplaceOnly: true` in the reinstall branch.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16 :: top-level rei completes to reinstall with trailing space`.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16 :: reinstall @ completes marketplace-only targets`.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16 :: reinstall plugin half preserves multi-marketplace no trailing space`.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16 :: reinstall flag completion includes --force only for reinstall`.
    - `node --test tests/edge/completions/provider.test.ts` exits 0.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden completion failure-semantics regression coverage</name>
  <files>tests/edge/completions/provider.test.ts</files>
  <read_first>
    - tests/edge/completions/provider.test.ts
    - extensions/pi-claude-marketplace/edge/completions/provider.ts
    - extensions/pi-claude-marketplace/edge/completions/data.ts
    - extensions/pi-claude-marketplace/shared/completion-cache.ts
    - .planning/phases/09-reinstall-edge-bulk-ux/09-VALIDATION.md
  </read_first>
  <action>
    Extend `tests/edge/completions/provider.test.ts` to prove reinstall preserves existing completion failure semantics.

    Concrete tests to add or extend:
    - Add `PRL-16 :: reinstall per-marketplace manifest soft-fail preserves other marketplace completions`: configure two marketplaces in fixture state, one throwing from `loadManifestForMarketplace` and one returning `[{ name: "ok", status: "installed" }]`; assert `getArgumentCompletions("reinstall ", resolver)` returns an item labeled `ok@<marketplace>` and does not throw.
    - Add `PRL-16 :: reinstall state load errors propagate`: configure `stateThrows.user = new Error("state boom")`; assert `await assert.rejects(() => getArgumentCompletions("reinstall ", resolver), /state boom/)`.
    - Add assertions that terminal reinstall completions include trailing spaces: `reinstall ` unique plugin ref value ends with one space, `reinstall @` marketplace-only value ends with one space, and `reinstall shared` multi-marketplace half completion value is exactly `reinstall shared@` with no trailing space.
    - Keep existing update/uninstall/install failure-semantics tests unchanged.
  </action>
  <verify>
    <automated>node --test tests/edge/completions/provider.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/edge/completions/provider.test.ts` contains `PRL-16 :: reinstall per-marketplace manifest soft-fail preserves other marketplace completions`.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16 :: reinstall state load errors propagate`.
    - `tests/edge/completions/provider.test.ts` contains `reinstall shared@`.
    - `tests/edge/completions/provider.test.ts` contains `state boom`.
    - `node --test tests/edge/completions/provider.test.ts` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Files to Modify

- `extensions/pi-claude-marketplace/edge/completions/provider.ts` - add top-level reinstall completion, ref completion branch, reinstall-only `--force` flag completion, and `--force` positional skipping hookup.
- `extensions/pi-claude-marketplace/edge/completions/data.ts` - add reinstall plugin-ref completion mode and boolean-flag skip support for positionals.
- `tests/edge/completions/provider.test.ts` - add PRL-16 coverage for top-level, refs, marketplace-only form, `--force`, trailing spaces, soft-fail, and state-error behavior.

## New Files

None.

## Dependencies

- This plan is code-independent of 09-01 and 09-02; it can execute in wave 2 in parallel with router/handler work.
- 09-04 depends on this plan for docs and full validation.

## Verification Criteria

Run:

```bash
node --test tests/edge/completions/provider.test.ts
npm run typecheck
```

## Risks

- Generalizing `extractPositionals` must not break list filter completions; use the optional `booleanFlags` parameter only for reinstall.
- Marketplace-only completion currently returns all marketplace names, not only marketplaces with installed plugins. This plan deliberately preserves update parity per Phase 9 decision D-16.
