---
phase: 09-reinstall-edge-bulk-ux
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - README.md
  - tests/architecture/reinstall-docs.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/edge/handlers/plugin/reinstall.test.ts
  - tests/edge/router.test.ts
  - tests/edge/register.test.ts
  - tests/edge/completions/provider.test.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PRL-01
  - PRL-03
  - PRL-04
  - PRL-05
  - PRL-13
  - PRL-14
  - PRL-15
  - PRL-16
must_haves:
  truths:
    - "README documents all three reinstall target forms."
    - "README documents --scope user|project and reinstall-specific --force."
    - "README states reinstall uses cached manifests, performs no network sync, preserves the recorded version, targets installed plugins only, and deletes plugin data only after successful replacement."
    - "Static validation proves the README contains the reinstall syntax and semantic notes."
    - "Full focused Phase 9 validation and npm run check are green before phase completion."
    - "Planning traceability marks PRL-01, PRL-03, PRL-04, PRL-05, PRL-13, PRL-14, PRL-15, and PRL-16 complete after implementation validation."
  artifacts:
    - path: "README.md"
      provides: "User-facing reinstall command reference"
    - path: "tests/architecture/reinstall-docs.test.ts"
      provides: "Static README contract coverage for reinstall UX"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 9 requirement completion checkboxes"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 completion and success criteria traceability"
  key_links:
    - from: "README.md"
      to: "extensions/pi-claude-marketplace/edge/router.ts"
      via: "same reinstall usage syntax"
      pattern: "/claude:plugin reinstall [<plugin>@<marketplace> | @<marketplace>] [--scope user|project] [--force]"
    - from: "tests/architecture/reinstall-docs.test.ts"
      to: "README.md"
      via: "readFile static assertions"
      pattern: "cached manifests"
---

# Implementation Plan

## Goal
Document reinstall UX and close Phase 9 with static docs coverage, full validation, and requirement traceability updates.

## Tasks

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update README reinstall command documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/phases/09-reinstall-edge-bulk-ux/09-CONTEXT.md
    - .planning/phases/09-reinstall-edge-bulk-ux/09-RESEARCH.md
    - extensions/pi-claude-marketplace/edge/router.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts
  </read_first>
  <action>
    Update `README.md` user-facing documentation for PRL-01/03/04/05/13/14/15/16.

    Concrete edits:
    - In the `/claude:plugin` reference introduction, replace `after installing, uninstalling, or updating plugins` with `after installing, uninstalling, updating, or reinstalling plugins`.
    - In the `### Plugin` section, immediately after the existing update examples block and before `Uninstall a plugin:`, add this exact paragraph and code block:
      ```markdown
      Reinstall one installed plugin, every installed plugin from one marketplace, or all installed plugins. Reinstall uses the cached marketplace manifest and the installed record's existing version; it does not fetch, pull, or otherwise sync the marketplace from the network:

      ```text
      /claude:plugin reinstall pr-review-toolkit@claude-plugins-official
      /claude:plugin reinstall @claude-plugins-official
      /claude:plugin reinstall
      ```
      ```
    - Immediately after that block, add this exact paragraph and code block:
      ```markdown
      Limit reinstall to one scope with `--scope user` or `--scope project`. The flag can appear before or after the target:

      ```text
      /claude:plugin reinstall --scope project
      /claude:plugin reinstall @claude-plugins-official --scope user
      ```
      ```
    - Immediately after that block, add this exact paragraph and code block:
      ```markdown
      Use `--force` only when reinstalling a plugin whose own previous agent files were manually edited or otherwise look foreign. `--force` can overwrite that plugin's previous agent content, but it does not override other-plugin ownership conflicts, unsafe names, path-containment failures, or MCP server name collisions:

      ```text
      /claude:plugin reinstall pr-review-toolkit@claude-plugins-official --force
      ```
      ```
    - Immediately after the `--force` example, add this exact paragraph:
      ```markdown
      Reinstall targets installed plugins only. If no installed plugins match the selected target set, the command reports `No plugins installed.` and does not emit a reload hint. Plugin data directories are deleted only after replacement resources and `state.json` commit successfully; if reinstall fails, the previous plugin state, resources, and data remain available.
      ```
    - Do not say reinstall installs absent plugins or fetches latest marketplace content.
  </action>
  <verify>
    <automated>grep -n "claude:plugin reinstall" README.md && grep -n "cached marketplace manifest" README.md && grep -n "No plugins installed" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `/claude:plugin reinstall pr-review-toolkit@claude-plugins-official`.
    - `README.md` contains `/claude:plugin reinstall @claude-plugins-official`.
    - `README.md` contains `/claude:plugin reinstall --scope project`.
    - `README.md` contains `/claude:plugin reinstall pr-review-toolkit@claude-plugins-official --force`.
    - `README.md` contains `cached marketplace manifest`.
    - `README.md` contains `does not fetch, pull, or otherwise sync the marketplace from the network`.
    - `README.md` contains `installed record's existing version`.
    - `README.md` contains `Reinstall targets installed plugins only.`.
    - `README.md` contains `No plugins installed.`.
    - `README.md` contains `Plugin data directories are deleted only after replacement resources and `state.json` commit successfully`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add static README contract test for reinstall docs</name>
  <files>tests/architecture/reinstall-docs.test.ts, README.md</files>
  <read_first>
    - README.md
    - tests/architecture/no-orchestrator-network.test.ts
    - tests/architecture/index-smoke.test.ts
    - package.json
    - .planning/phases/09-reinstall-edge-bulk-ux/09-VALIDATION.md
  </read_first>
  <action>
    Create `tests/architecture/reinstall-docs.test.ts` to keep the reinstall docs contract under `npm test`.

    Concrete test file contents:
    - Import `assert` from `node:assert/strict`.
    - Import `readFile` from `node:fs/promises`.
    - Import `path` from `node:path`.
    - Import `test` from `node:test`.
    - Import `fileURLToPath` from `node:url`.
    - Define `const REPO_ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "../..");`.
    - Add one test named `PRL-01/03/04/05/13/14/15/16: README documents reinstall command forms and semantics`.
    - Read `README.md` as UTF-8.
    - Assert `readme.includes(...)` for these exact strings:
      - `/claude:plugin reinstall pr-review-toolkit@claude-plugins-official`
      - `/claude:plugin reinstall @claude-plugins-official`
      - `/claude:plugin reinstall`
      - `/claude:plugin reinstall --scope project`
      - `/claude:plugin reinstall pr-review-toolkit@claude-plugins-official --force`
      - `cached marketplace manifest`
      - `does not fetch, pull, or otherwise sync the marketplace from the network`
      - `installed record's existing version`
      - `Reinstall targets installed plugins only.`
      - `No plugins installed.`
      - `does not emit a reload hint`
      - `Plugin data directories are deleted only after replacement resources and `state.json` commit successfully`
    - Keep the file under `tests/architecture/` so the existing `npm test` glob picks it up.
  </action>
  <verify>
    <automated>node --test tests/architecture/reinstall-docs.test.ts tests/architecture/no-orchestrator-network.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/architecture/reinstall-docs.test.ts` exists.
    - `tests/architecture/reinstall-docs.test.ts` contains `README documents reinstall command forms and semantics`.
    - `tests/architecture/reinstall-docs.test.ts` contains `cached marketplace manifest`.
    - `tests/architecture/reinstall-docs.test.ts` contains `does not emit a reload hint`.
    - `tests/architecture/reinstall-docs.test.ts` contains `state.json`.
    - `node --test tests/architecture/reinstall-docs.test.ts tests/architecture/no-orchestrator-network.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run full Phase 9 validation and harden any missed coverage</name>
  <files>tests/orchestrators/plugin/reinstall.test.ts, tests/edge/handlers/plugin/reinstall.test.ts, tests/edge/router.test.ts, tests/edge/register.test.ts, tests/edge/completions/provider.test.ts, tests/architecture/no-orchestrator-network.test.ts, tests/architecture/reinstall-docs.test.ts</files>
  <read_first>
    - .planning/phases/09-reinstall-edge-bulk-ux/09-VALIDATION.md
    - .planning/phases/09-reinstall-edge-bulk-ux/09-CONTEXT.md
    - tests/orchestrators/plugin/reinstall.test.ts
    - tests/edge/handlers/plugin/reinstall.test.ts
    - tests/edge/router.test.ts
    - tests/edge/register.test.ts
    - tests/edge/completions/provider.test.ts
    - tests/architecture/no-orchestrator-network.test.ts
    - tests/architecture/reinstall-docs.test.ts
    - package.json
  </read_first>
  <action>
    Run the full Phase 9 validation suite and add concrete missing assertions if any PRL behavior is not already covered.

    Required commands, in order:
    ```bash
    node --test tests/orchestrators/plugin/reinstall.test.ts tests/architecture/no-orchestrator-network.test.ts
    node --test tests/edge/handlers/plugin/reinstall.test.ts tests/edge/router.test.ts tests/edge/register.test.ts
    node --test tests/edge/completions/provider.test.ts
    node --test tests/architecture/reinstall-docs.test.ts
    npm run typecheck
    npm run check
    ```

    Required coverage strings after hardening:
    - `tests/orchestrators/plugin/reinstall.test.ts` contains `PRL-03`, `PRL-04`, `PRL-05`, `PRL-13`, `PRL-14`, and `PRL-15`.
    - `tests/edge/handlers/plugin/reinstall.test.ts` contains `--force`, `--scope project`, `Too many arguments`, and `Unknown option`.
    - `tests/edge/router.test.ts` contains `reinstall [<plugin>@<marketplace> | @<marketplace>] [--scope user|project] [--force]`.
    - `tests/edge/register.test.ts` contains `reinstall plugins`.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16`, `reinstall @`, and `--force`.
    - `tests/architecture/no-orchestrator-network.test.ts` still contains `orchestrators/plugin/reinstall.ts` and `refreshGitHubClone reference`.
    - `tests/architecture/reinstall-docs.test.ts` contains `PRL-01/03/04/05/13/14/15/16`.

    If any command fails due to missing assertions rather than production behavior, add the smallest targeted test assertion to the file named by the failed coverage string above. Do not weaken existing assertions. Do not remove the no-network architecture guard.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/reinstall.test.ts tests/architecture/no-orchestrator-network.test.ts && node --test tests/edge/handlers/plugin/reinstall.test.ts tests/edge/router.test.ts tests/edge/register.test.ts && node --test tests/edge/completions/provider.test.ts && node --test tests/architecture/reinstall-docs.test.ts && npm run typecheck && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `tests/orchestrators/plugin/reinstall.test.ts` contains `PRL-03`.
    - `tests/orchestrators/plugin/reinstall.test.ts` contains `PRL-04`.
    - `tests/orchestrators/plugin/reinstall.test.ts` contains `PRL-05`.
    - `tests/orchestrators/plugin/reinstall.test.ts` contains `PRL-13`.
    - `tests/orchestrators/plugin/reinstall.test.ts` contains `PRL-14`.
    - `tests/orchestrators/plugin/reinstall.test.ts` contains `PRL-15`.
    - `tests/edge/handlers/plugin/reinstall.test.ts` contains `Too many arguments`.
    - `tests/edge/handlers/plugin/reinstall.test.ts` contains `Unknown option`.
    - `tests/edge/completions/provider.test.ts` contains `PRL-16`.
    - `npm run check` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update planning traceability after green validation</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/09-reinstall-edge-bulk-ux/09-CONTEXT.md
    - .planning/phases/09-reinstall-edge-bulk-ux/09-VALIDATION.md
  </read_first>
  <action>
    After `npm run check` is green, update planning traceability for Phase 9 completion.

    Concrete edits:
    - In `.planning/REQUIREMENTS.md`, change these requirement checkboxes from `[ ]` to `[x]`: PRL-01, PRL-03, PRL-04, PRL-05, PRL-13, PRL-14, PRL-15, PRL-16.
    - In `.planning/REQUIREMENTS.md` Traceability table, change these rows from `Pending` to `Complete`: PRL-01, PRL-03, PRL-04, PRL-05, PRL-13, PRL-14, PRL-15, PRL-16.
    - Append a final line at the bottom of `.planning/REQUIREMENTS.md`: `*Last updated: 2026-05-14 after Phase 9 completion*`.
    - In `.planning/ROADMAP.md`, change Phase 9 checkbox from `[ ]` to `[x]`.
    - In `.planning/ROADMAP.md` Phase 9 plan count, set `Plans: 4 plans` and list:
      - `[x] \`09-01-PLAN.md\` -- Bulk reinstall orchestrator, quiet seam, deterministic summary output`
      - `[x] \`09-02-PLAN.md\` -- Reinstall edge handler, router, registration, --scope, and --force`
      - `[x] \`09-03-PLAN.md\` -- Reinstall tab completion and failure semantics`
      - `[x] \`09-04-PLAN.md\` -- README docs, static docs test, full validation, and traceability`
    - In `.planning/ROADMAP.md` progress table, set Phase 9 `Plans` to `4/4`, `Status` to `Complete`, and `Completed` to `2026-05-14`.
    - In `.planning/ROADMAP.md` Coverage table, set PRL-01, PRL-03, PRL-04, PRL-05, PRL-13, PRL-14, PRL-15, and PRL-16 to `Complete`.
    - In `.planning/STATE.md` frontmatter, set `status: completed`, nested `progress.completed_phases: 2`, nested `progress.total_plans: 8`, nested `progress.completed_plans: 8`, and nested `progress.percent: 100`.
    - In `.planning/STATE.md`, update Current Position to `Phase: 09 of 2 (reinstall edge & bulk UX)`, `Plan: 09-04`, `Status: Complete`, and `Progress: [██████████] 100%`.
    - Add a decision bullet in `.planning/STATE.md` Accumulated Context Decisions: `[Phase 09]: reinstallPlugins provides update-analogous bulk target forms, deterministic partitions, reload-hint aggregation, soft-dependency aggregation, and quiet single-plugin rendering for batch UX. -- Plan 09-01 completed PRL-03/04/05/13/14/15.`
    - Add a decision bullet: `[Phase 09]: /claude:plugin reinstall is routed, registered, documented, and completed with installed-only tab completion plus reinstall-specific --force. -- Plans 09-02/09-03/09-04 completed PRL-01/16 and final validation.`
  </action>
  <verify>
    <automated>grep -n "PRL-16.*Complete" .planning/REQUIREMENTS.md .planning/ROADMAP.md && grep -n "Progress: \[██████████\] 100%" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `[x] **PRL-01**`.
    - `.planning/REQUIREMENTS.md` contains `[x] **PRL-16**`.
    - `.planning/REQUIREMENTS.md` contains `PRL-16 | Phase 9 | Complete`.
    - `.planning/ROADMAP.md` contains `09-04-PLAN.md`.
    - `.planning/ROADMAP.md` contains `| 9. Reinstall Edge & Bulk UX | Command routing, batch forms, scope, completion, output, docs | PRL-01, PRL-03, PRL-04, PRL-05, PRL-13, PRL-14, PRL-15, PRL-16 | 4/4 plans | Complete | 2026-05-14 |`.
    - `.planning/STATE.md` contains `total_plans: 8`.
    - `.planning/STATE.md` contains `percent: 100`.
    - `.planning/STATE.md` contains `Progress: [██████████] 100%`.
    - `.planning/STATE.md` contains `reinstallPlugins provides update-analogous bulk target forms`.
  </acceptance_criteria>
</task>

</tasks>

## Files to Modify

- `README.md` - add reinstall syntax, `--scope`, `--force`, cached/no-network/version/data semantics, and reload/no-target notes.
- `tests/architecture/reinstall-docs.test.ts` - static docs contract test included in the default test glob.
- `tests/orchestrators/plugin/reinstall.test.ts` - only if final coverage hardening finds missing Phase 9 requirement assertions.
- `tests/edge/handlers/plugin/reinstall.test.ts` - only if final coverage hardening finds missing handler assertions.
- `tests/edge/router.test.ts` - only if final coverage hardening finds missing usage assertion.
- `tests/edge/register.test.ts` - only if final coverage hardening finds missing registration assertion.
- `tests/edge/completions/provider.test.ts` - only if final coverage hardening finds missing PRL-16 assertions.
- `.planning/REQUIREMENTS.md` - mark Phase 9 requirements complete after green validation.
- `.planning/ROADMAP.md` - mark Phase 9 complete and list four plans.
- `.planning/STATE.md` - update milestone progress and completion decisions.

## New Files

- `tests/architecture/reinstall-docs.test.ts` - static README assertions for reinstall command forms and semantics.

## Dependencies

- Depends on 09-01, 09-02, and 09-03 because docs and final validation must reflect the final API, handler syntax, and completion behavior.
- Task 4 depends on Tasks 1-3 and must only run after `npm run check` exits 0.

## Verification Criteria

Run the full sequence:

```bash
node --test tests/orchestrators/plugin/reinstall.test.ts tests/architecture/no-orchestrator-network.test.ts
node --test tests/edge/handlers/plugin/reinstall.test.ts tests/edge/router.test.ts tests/edge/register.test.ts
node --test tests/edge/completions/provider.test.ts
node --test tests/architecture/reinstall-docs.test.ts
npm run typecheck
npm run check
```

## Risks

- Planning traceability should not be marked complete until the full validation sequence is green.
- README wording must not imply reinstall fetches latest marketplace content; use update/marketplace update for network refresh semantics.
- `--force` docs must stay narrow: it only affects this plugin's previous agent content and must not claim to override MCP collisions or path safety.
