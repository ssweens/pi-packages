---
phase: 05-plugin-orchestrators
plan: 10
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - CHANGELOG.md
autonomous: true
requirements:
  - "D-07-COMP-01"
tags: [phase-05, documentation, supersession]
must_haves:
  truths:
    - "REQUIREMENTS.md PR-4 row is marked SUPERSEDED with strikethrough; superseding decision (Phase 5 D-07) named in the strikethrough note"
    - "PROJECT.md Key Decisions table gains a new row (D-24 in project-wide decision numbering) documenting the COMP-01 supersession"
    - "CHANGELOG.md (or equivalent) gains a 'behavior corrected vs V1' entry per CONTEXT.md D-07 supersession effect"
    - "PRD §6.4 PR-4 retains the original text as historical baseline (NOT modified) -- the supersession lives in .planning/ artifacts only"
    - "npm run check is green (these are documentation edits; should not affect typecheck/lint/tests)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PR-4 strikethrough + supersession note"
      contains: "superseded by Phase 5 D-07"
    - path: ".planning/PROJECT.md"
      provides: "D-24 row in Key Decisions table"
      contains: "COMP-01"
    - path: "CHANGELOG.md"
      provides: "Behavior-corrected-vs-V1 entry"
      contains: "COMP-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md PR-4 row"
      to: ".planning/PROJECT.md D-24 row"
      via: "supersession pattern mirroring Phase 1 D-21 (MA-7) and Phase 4 D-23 (MU-2/MU-3)"
      pattern: "superseded by"
---

<objective>
Land the three documentation edits that close out D-07 (COMP-01 supplement-not-replace) per the established supersession pattern (Phase 1 D-21 / Phase 4 D-23): (1) REQUIREMENTS.md PR-4 strikethrough with note, (2) PROJECT.md Key Decisions table new row (D-24 in project-wide numbering), (3) CHANGELOG.md "behavior corrected vs V1" entry. PRD §6.4 PR-4 is INTENTIONALLY left untouched (the supersession lives in `.planning/` artifacts per CONTEXT.md D-07 supersession effect).

Purpose: D-07 ships behavior-corrected-vs-V1 (custom componentPath arrays SUPPLEMENT defaults rather than replace them). The behavior change landed in Plan 05-03. This plan only ships the DOCUMENTATION supersession trail so future readers tracing PR-4's status see the deliberate change.

Output: Three edits to existing docs; no new files.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-plugin-orchestrators/05-CONTEXT.md
@.planning/phases/05-plugin-orchestrators/05-RESEARCH.md
@.planning/phases/05-plugin-orchestrators/05-03-SUMMARY.md

@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strikethrough PR-4 in REQUIREMENTS.md with supersession note</name>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <action>
Open `.planning/REQUIREMENTS.md`. Locate the line near "### Plugin Compatibility Resolver (PRD §6.4)" reading:

```
- [ ] **PR-4**: Detect implicit components by convention only when corresponding manifest field absent
```

Replace with strikethrough + supersession note mirroring the existing pattern at MA-7 (line ~17) and MU-2/MU-3 (lines ~44-45):

```
- [x] ~~**PR-4**: Detect implicit components by convention only when corresponding manifest field absent~~ (**superseded by Phase 5 D-07**: custom componentPath arrays now SUPPLEMENT defaults rather than replace them; implicit-by-convention is always detected when the conventional dir exists. Behavior corrected vs V1 per Gap 3 / COMP-01. This is a deliberate user-contract change recorded in PROJECT.md Key Decisions.)
```

Verify the strikethrough syntax matches the EXACT format used at MA-7 and MU-2 (the existing supersessions use `~~text~~` for the original line and `(**superseded by ...**: explanation)` for the note; bold-italicize and parenthesize the same way).

Do NOT modify PRD §6.4 PR-4 -- per CONTEXT.md D-07 supersession effect, "PRD §6.4 PR-4 retains the original text as historical baseline; the supersession lives in `.planning/` artifacts only."
  </action>
  <verify>
    <automated>grep -E 'superseded by Phase 5 D-07' .planning/REQUIREMENTS.md | grep -c "PR-4"</automated>
  </verify>
  <done>
    - PR-4 line is marked with `~~strikethrough~~` and `- [x]` checkbox
    - Supersession note names "Phase 5 D-07" and references COMP-01
    - Format matches existing MA-7 / MU-2 / MU-3 supersession style
    - PRD §6.4 PR-4 NOT modified
  </done>
</task>

<task type="auto">
  <name>Task 2: Add D-24 row to PROJECT.md Key Decisions table</name>
  <files>
    - .planning/PROJECT.md
  </files>
  <action>
Open `.planning/PROJECT.md` and locate the "Key Decisions" table. Find the most-recent decision row (likely D-23 -- Phase 4 MU-2/MU-3 supersession per Plan 04-10). Add a new row immediately after the most-recent decision:

```
| D-24 | Phase 5 -- 2026-05-10 | COMP-01 (Gap 3) supplement-not-replace: custom componentPath arrays SUPPLEMENT defaults rather than replace them | PR-4 superseded; resolver `ComponentPathsSchema` migrated from optional-string-per-kind to readonly-string-array-per-kind; strict resolver Step 7 computes UNION of declared (entry > manifest) + implicit-by-convention with first-wins dedup; bridge discover.ts files iterate the array. Behavior corrected vs V1. |
```

Notes:

- The EXACT format of the table (columns, header row) must mirror the existing rows. Read 2-3 existing rows before writing the new one.
- The decision ID is `D-24` per project-wide numbering. If a recent plan landed `D-25`+ already, adjust to the next available ID.
- The date is `2026-05-10` (Phase 5 plan date).
- Cross-reference: link to `.planning/phases/05-plugin-orchestrators/05-CONTEXT.md` decision D-07 (the phase-internal decision name).
  </action>
  <verify>
    <automated>grep -c "D-24\|COMP-01.*supplement" .planning/PROJECT.md</automated>
  </verify>
  <done>
    - New row in Key Decisions table with ID D-24 (or next available)
    - Row references COMP-01 / Gap 3 / PR-4 supersession
    - Format mirrors existing rows in the table
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CHANGELOG entry for behavior-corrected-vs-V1</name>
  <files>
    - CHANGELOG.md
  </files>
  <action>
Open `CHANGELOG.md` at the repository root. If the file does not exist, create it with a minimal Keep-a-Changelog header. If it exists, locate the most-recent unreleased section (typically `## [Unreleased]`) or create one.

Add an entry under a `### Changed` (or equivalent) subsection:

```
### Changed

- **Behavior corrected vs V1 (COMP-01 / Gap 3):** Custom component-path arrays in plugin manifests (`componentPaths.skills`, `.commands`, `.agents`) now SUPPLEMENT the implicit-by-convention defaults rather than replace them. Previously, declaring `componentPaths.skills: "custom/skills"` would suppress detection of the default `skills/` directory; with this change, both the declared path AND the default convention path are included (deduplicated, first-wins on collisions). Resolver schema migrated from optional-string-per-kind to readonly-string-array-per-kind. PRD §6.4 PR-4 superseded by Phase 5 D-07. See `.planning/PROJECT.md` Key Decisions row D-24.
```

If `CHANGELOG.md` doesn't exist in the repo (verify with `ls CHANGELOG.md`), create it from scratch with:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Changed

- **Behavior corrected vs V1 (COMP-01 / Gap 3):** Custom component-path arrays in plugin manifests (...) [see above].
```

Notes:

- The exact section heading style and ordering depends on whether the repo already has CHANGELOG entries from Phase 1-4. Mirror whatever exists.
- If Phase 1 D-21 (MA-7 isomorphic-git supersession) or Phase 4 D-23 (MU-2/MU-3 follow-upstream-blindly) landed their own CHANGELOG entries, mirror those format choices for consistency.
- The reference to "PROJECT.md Key Decisions row D-24" should match the actual decision ID landed in Task 2.
  </action>
  <verify>
    <automated>grep -c "COMP-01\|behavior corrected" CHANGELOG.md</automated>
  </verify>
  <done>
    - `CHANGELOG.md` exists at repo root (created if necessary)
    - Entry under `### Changed` (or equivalent) names COMP-01 / Gap 3
    - Entry references PROJECT.md D-24 (or whatever ID landed in Task 2)
    - Format mirrors any existing CHANGELOG entries
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation artifacts | Documentation edits have no runtime trust boundary; the concern here is consistency and auditability of user-contract changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-20 | Repudiation | Future contributor implements PR-4 short-circuit, breaking COMP-01 | mitigate | The supersession trail across REQUIREMENTS.md + PROJECT.md + CHANGELOG.md + Plan 05-03 SUMMARY + Plan 05-03 source comments + `tests/domain/resolver-comp01.test.ts` provides 5 independent records of the decision. A regression to the V1 behavior would be detected at test-time by the COMP-01 fixture-c assertion (Plan 05-03). |
</threat_model>

<verification>
- `npm run check` is green (documentation edits do not affect typecheck/lint/tests)
- `grep "superseded by Phase 5 D-07" .planning/REQUIREMENTS.md` returns the PR-4 line
- `grep "COMP-01" .planning/PROJECT.md` returns the D-24 row
- `grep "COMP-01\|behavior corrected" CHANGELOG.md` returns the changelog entry
</verification>

<success_criteria>
- All three documentation edits land per the established supersession pattern (Phase 1 D-21 / Phase 4 D-23)
- PRD §6.4 PR-4 is INTENTIONALLY NOT modified
- Cross-references between the three docs are consistent (e.g., D-24 referenced from CHANGELOG, COMP-01 referenced from REQUIREMENTS PR-4 row)
</success_criteria>

<output>
After completion, create `.planning/phases/05-plugin-orchestrators/05-10-SUMMARY.md` summarizing the three edits, the cross-references, and the decision to leave PRD §6.4 untouched.
</output>

<self_check>
- [ ] REQUIREMENTS.md PR-4 strikethrough + supersession note format mirrors MA-7 / MU-2 style
- [ ] PROJECT.md D-24 row added with date 2026-05-10 + COMP-01 reference
- [ ] CHANGELOG.md entry under `### Changed` references COMP-01 / Gap 3 / PROJECT.md D-24
- [ ] PRD §6.4 PR-4 NOT modified (supersession lives in `.planning/` artifacts only per CONTEXT.md D-07)
- [ ] Decision ID D-24 is the next available; verify against PROJECT.md existing rows
- [ ] `npm run check` green
</self_check>
